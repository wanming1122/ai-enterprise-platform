# 企业管理系统开发文档

版本：v1.0

更新时间：2026-09-04

配套设计文档：《组织架构模块设计方案》v4.1（本目录下，含组织架构全量设计与"十一、RAG知识库模块设计"章节）、《实训成果物要求.md》（验收总纲）

本文档用途：作为AI开发Agent的执行蓝本，按里程碑顺序开发，每个任务完成后须通过对应验收标准方可进入下一任务。

## 〇、给开发Agent的使用说明

1. 工作目录为本文档所在目录，先完整阅读《组织架构模块设计方案.md》再动手；两份文档冲突时以《组织架构模块设计方案》为准，仍不明确的按本文档执行。
2. 技术栈固定为：前端 React 18 + TypeScript + Ant Design 5 + Zustand + React Router + Vite；后端 FastAPI + SQLAlchemy + Alembic + PyJWT + Passlib(bcrypt)；数据库 MySQL 8（utf8mb4）；RAG 向量库 Chroma；Agent 框架 LangGraph；生成模型 MiMo-V2.5（OpenAI兼容API）。不得擅自更换框架或引入重型中间件（Redis、Docker、消息队列均不需要）。
3. 严格按里程碑 M0→M5 顺序执行，每个里程碑内的任务按编号顺序执行；每完成一个任务先自测通过验收标准，再继续。
4. 全局硬性规则：所有删除功能一律软删除（status标记），前端删除后仅不再显示，数据不物理清除；所有管理类接口必须绑定权限校验；所有列表接口必须分页。
5. 每完成一个里程碑做一次git提交，提交信息格式：`里程碑编号-任务编号: 概要`，如 `M1-T3: 用户管理CRUD`。
6. 涉及外部API密钥一律走.env配置，严禁硬编码；文档与代码注释中不得出现真实密钥。

## 一、项目概述

企业管理系统实训项目，包含组织管理（员工、部门、职位、考勤、薪资）、个人中心、AI智能中心（AI助手、NL2SQL、模型配置）、RAG知识库四大板块。系统以组织架构模块为基础平台（认证、RBAC、动态菜单），业务模块与AI模块在其上扩展。

核心业务规则：

+ 员工管理：可增删改查的员工列表，基本属性含姓名、手机、邮箱、归属部门、职位、在职状态。
+ 部门管理：预置技术部、销售部、人事行政、产品、后勤采购、财务六个部门，支持树形增删改查，具备部门经理、员工数量、描述、状态属性。
+ 职位管理：不同职位薪资不同、系统权限不同（绑定角色）、有描述信息，可增删改查。
+ 考勤管理：导入电子考勤表（员工、部门、日期、签到、签退、状态、地点），据此对当月薪资增减。
+ 薪资管理：薪资 = 职位基本工资 + 考勤增减 + 手动奖励/罚款，构成详细数据。
+ NL2SQL：录入产品数据，自然语言生成SQL，人工审核通过后执行，保留查询历史。
+ AI助手：类豆包Web端对话，具备RAG知识库检索与多模态能力，基于LangGraph编排。

## 二、环境要求与依赖清单

环境：Python 3.11+、Node.js 18+、MySQL 8.x（字符集utf8mb4）。

后端核心依赖（requirements.txt）：fastapi、uvicorn[standard]、sqlalchemy、pymysql、alembic、pydantic、pydantic-settings、pyjwt、passlib[bcrypt]、python-multipart、pandas、openpyxl、pdfplumber、python-docx、chromadb、langgraph、langchain-core、langchain-openai、httpx、aiosqlite（仅测试用，可选）。

前端核心依赖：react、react-dom、typescript、antd、@ant-design/icons、zustand、react-router-dom、axios、dayjs。

.env模板（backend/.env）：

```
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=enterprise
JWT_SECRET=随机长字符串
JWT_ACCESS_EXPIRE_MINUTES=30
JWT_REFRESH_EXPIRE_DAYS=7
MEDIA_DIR=./media
CHROMA_DIR=./data/chroma
ZHIPU_API_KEY=            # 默认Embedding（智谱embedding-3）
MIMO_BASE_URL=            # MiMo-V2.5接入地址（OpenAI兼容）
MIMO_API_KEY=
```

启动命令：后端 `uvicorn app.main:app --reload --port 8000`；前端 `npm run dev`（Vite默认5173端口）；接口文档 `http://127.0.0.1:8000/docs`。

## 三、项目目录结构

```
demo/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI入口，注册路由/CORS/异常处理
│   │   ├── core/                # config.py(.env读取)、security.py(JWT/bcrypt)、deps.py(认证与权限依赖)
│   │   ├── db/                  # session.py、base.py
│   │   ├── models/              # SQLAlchemy模型，一个模块一个文件
│   │   ├── schemas/             # Pydantic模型，一个模块一个文件
│   │   ├── routers/             # APIRouter，一个模块一个文件
│   │   ├── services/            # 业务逻辑层（user/department/menu/role/attendance/salary/kb/ai）
│   │   └── utils/               # 统一响应、分页、审计日志混入、文件校验
│   ├── alembic/                 # 迁移脚本
│   ├── media/                   # 知识库原始文件存储
│   ├── data/chroma/             # Chroma持久化目录
│   ├── requirements.txt
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── api/                 # axios封装 + 各模块接口
│   │   ├── types/               # TS类型定义
│   │   ├── stores/              # zustand：user.ts（令牌/用户/菜单/权限）
│   │   ├── router/              # 静态路由 + 动态路由生成
│   │   ├── components/          # HasPermission、usePermission、表格/弹窗封装
│   │   └── views/               # 页面，按模块分目录
│   └── package.json
├── 实训成果物要求.md
├── 组织架构模块设计方案.md
└── 开发文档.md
```

## 四、全局开发规范

1. 接口统一返回 `{code: int, message: str, data: any}`，code=0成功，401未认证，403无权限，422参数错误，500服务器错误；分页返回 `data: {list, total, page, page_size}`。
2. 认证：除登录、注册、找回密码、公开邀请等接口外，一律Bearer Token；双Token机制（access 30分钟 / refresh 7天，刷新轮换）。
3. 权限：管理类接口通过依赖注入校验权限码，无权限返回403并写审计日志；前端按钮用 `<HasPermission code="xxx">` 控制显隐。
4. 命名：后端模型蛇形（sys_user），路由复数资源式（/api/v1/users）；前端组件大驼峰，页面views下按模块小写目录。
5. 软删除：所有表带status字段（1正常/启用，2软删除，个别表0停用），查询一律过滤status!=2。
6. 审计：登录、权限拦截、增删改、审批、导入、上传等关键操作写sys_log（操作人、模块、类型、地址、方法、参数、IP、结果、耗时）。
7. 时间统一存DATETIME，前端展示用dayjs格式化；金额用DECIMAL(10,2)，严禁浮点。

## 五、数据库设计总览

组织与权限核心表（sys_user、sys_department、sys_role、sys_user_role_relation、sys_menu、sys_role_menu_relation、sys_permission、sys_menu_permission_relation）、注册邀请与安全表（sys_registration_approval、sys_invitation、sys_invitation_log、sys_password_recovery_code、sys_log、sys_config、sys_dict_type、sys_dict_item）的表结构、字段说明、业务流程、接口设计、权限码，全部按《组织架构模块设计方案》第四至七章实现，此处不再重复。

RAG三张表（kb_knowledge_base、kb_file、kb_chunk）按设计方案"11.8 数据库表设计"实现。

以下为设计方案未覆盖、本文档补充的最小表设计：

### 5.1 职位管理表

sys_position职位表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(64) | NOT NULL, UNIQUE | 职位名称 |
| code | VARCHAR(32) | NOT NULL, UNIQUE | 职位编码，字母开头 |
| level | TINYINT | DEFAULT 1 | 职级 |
| base_salary | DECIMAL(10,2) | NOT NULL | 月基本工资，职位间薪资不同 |
| role_id | BIGINT | 可空 | 绑定角色，决定该职位用户的权限模板 |
| description | VARCHAR(255) | 可空 | 职位描述 |
| status | TINYINT | DEFAULT 1 | 1启用 0停用 2软删除 |
| created_at / updated_at | DATETIME | | |

sys_user表在组织模块设计基础上增加 position_id BIGINT 可空外键，员工管理界面选择职位。

### 5.2 考勤管理表

att_record考勤记录表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| user_id | BIGINT | NOT NULL | 员工，关联sys_user |
| dept_id | BIGINT | NOT NULL | 部门快照 |
| att_date | DATE | NOT NULL | 考勤日期 |
| check_in | TIME | 可空 | 签到时间 |
| check_out | TIME | 可空 | 签退时间 |
| status | VARCHAR(16) | NOT NULL | normal/late/early_leave/miss_check/absent/leave/business_trip |
| location | VARCHAR(128) | 可空 | 考勤地点 |
| remark | VARCHAR(255) | 可空 | 备注 |
| source | TINYINT | DEFAULT 1 | 1导入 2手动 |
| importer_id | BIGINT | 可空 | 导入人 |
| created_at | DATETIME | | |

唯一索引 (user_id, att_date)，同员工同日仅一条，重复导入覆盖更新。

att_rule考勤薪资规则表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK | 主键 |
| status_key | VARCHAR(16) | NOT NULL, UNIQUE | 对应att_record.status |
| adjust_type | TINYINT | NOT NULL | 1奖励 2扣款 |
| amount | DECIMAL(10,2) | NOT NULL | 每次/每日金额 |
| enabled | TINYINT | DEFAULT 1 | 是否启用 |
| updated_at | DATETIME | | |

预置数据：late扣50、early_leave扣50、absent扣200、miss_check扣30、leave/business_trip不动、normal不加不减。规则可在页面维护。

### 5.3 薪资管理表

sal_payroll月度工资单表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK | 主键 |
| user_id | BIGINT | NOT NULL | 员工 |
| year_month | CHAR(7) | NOT NULL | 所属月份，格式YYYY-MM |
| position_name | VARCHAR(64) | NOT NULL | 职位快照 |
| base_salary | DECIMAL(10,2) | NOT NULL | 职位基本工资快照 |
| attendance_adjust | DECIMAL(10,2) | DEFAULT 0 | 考勤增减合计 |
| manual_adjust | DECIMAL(10,2) | DEFAULT 0 | 手动奖惩合计 |
| total_salary | DECIMAL(10,2) | NOT NULL | 应发合计 |
| status | TINYINT | DEFAULT 0 | 0草稿 1已确认 2已发放 |
| operator_id | BIGINT | | 操作人 |
| confirmed_at | DATETIME | 可空 | 确认时间 |
| created_at / updated_at | DATETIME | | |

唯一索引 (user_id, year_month)。

sal_adjustment手动奖惩表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK | 主键 |
| user_id | BIGINT | NOT NULL | 员工 |
| adjust_type | TINYINT | NOT NULL | 1奖励 2罚款 |
| amount | DECIMAL(10,2) | NOT NULL | 金额 |
| reason | VARCHAR(255) | NOT NULL | 事由 |
| year_month | CHAR(7) | 可空 | 归属月份，生成工资单时汇总 |
| operator_id | BIGINT | NOT NULL | 操作人 |
| created_at | DATETIME | | |

生成逻辑：POST生成接口按月份汇总 base_salary（取用户当前职位）+ Σatt_rule（按当月考勤记录逐日计算）+ Σsal_adjustment（归属该月），写入sal_payroll；已确认的工资单不再重算。

### 5.4 AI智能中心表

ai_model模型配置表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK | 主键 |
| name | VARCHAR(64) | NOT NULL | 配置名称 |
| model_type | VARCHAR(16) | NOT NULL | llm / embedding / rerank |
| provider | VARCHAR(32) | NOT NULL | zhipu / dashscope / openai_compatible / local |
| base_url | VARCHAR(255) | 可空 | API地址 |
| api_key | VARCHAR(255) | NOT NULL | 加密存储（Fernet对称加密，密钥在.env） |
| model_name | VARCHAR(64) | NOT NULL | 模型标识，如MiMo-V2.5 |
| is_default | TINYINT | DEFAULT 0 | 同类型仅一个默认 |
| status | TINYINT | DEFAULT 1 | 1启用 0停用 2软删除 |
| created_at / updated_at | DATETIME | | |

product产品数据表（NL2SQL查询标的）：id、name VARCHAR(64)、category VARCHAR(32)、price DECIMAL(10,2)、stock INT、description VARCHAR(255)、status TINYINT、created_at/updated_at。

nl2sql_record查询记录表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK | 主键 |
| user_id | BIGINT | NOT NULL | 提问人 |
| question | TEXT | NOT NULL | 自然语言原句 |
| generated_sql | TEXT | NOT NULL | 生成的SQL |
| review_status | TINYINT | DEFAULT 0 | 0待审核 1通过 2驳回 3已执行 |
| review_comment | VARCHAR(255) | 可空 | 审核意见 |
| reviewer_id | BIGINT | 可空 | 审核人 |
| result_json | TEXT | 可空 | 执行结果JSON |
| execution_ms | INT | 可空 | 执行耗时 |
| executed_at | DATETIME | 可空 | 执行时间 |
| created_at | DATETIME | | |

ai_conversation会话表：id、user_id、title VARCHAR(64)、created_at、updated_at。
ai_message消息表：id、conversation_id、role VARCHAR(16)（user/assistant/tool）、content TEXT、tool_name VARCHAR(32)可空、citations JSON可空、created_at。

## 六、里程碑与任务分解

### M0 项目骨架（建议0.5天）

T1 初始化仓库与后端骨架：git init；创建backend，安装依赖；app/main.py挂载CORS（放行5173）、统一响应与全局异常处理、/api/v1/health健康检查；core/config.py读.env；db/session.py引擎与会话；alembic初始化并完成首个空迁移。验收：uvicorn启动，/docs可访问，/api/v1/health返回code=0。

T2 前端骨架：Vite创建react-ts项目；安装antd/zustand/react-router-dom/axios/dayjs；api/request.ts封装axios（拦截器：带Token、401跳登录、统一错误提示）；stores/user.ts；静态路由（登录页+占位首页）；主布局（侧边菜单+顶栏+内容区）。验收：npm run dev可登录页与占位首页切换，无控制台报错。

### M1 认证与RBAC（组织架构核心，建议3~4天）

T1 组织与权限表迁移：按设计方案第四章实现全部sys_*模型与迁移；预置数据：超级管理员账号、六个预置部门、基础角色、完整菜单树与权限标识（工作台/组织管理/操作日志/系统配置/个人中心及全部子菜单按钮）。验收：迁移执行成功，预置数据齐全。

T2 认证接口：登录（校验密码bcrypt、账号状态、失败计数与账号IP限流锁定）、令牌刷新（轮换）、当前用户信息+菜单树+权限列表、退出。前端完成登录页与动态路由：按菜单树生成路由、菜单渲染、令牌存储与刷新无感续登。验收：管理员登录见到完整菜单，错误密码5次锁定15分钟并有日志。

T3 用户管理：列表（多条件筛选分页）、新增、编辑、软删、启停、重置密码、Excel批量导入导出与模板下载。前端用户管理页面对齐设计文档8.1。验收：全流程可用，导入导出闭环，无权限操作返回403且按钮隐藏。

T4 部门管理：树形查询、增删改、启停、排序；约束：循环父子禁止、有子部门或绑定员工禁止删除（软删也不允许，提示先处理）。前端树形页面。验收：六个预置部门可见可维护，约束生效。

T5 角色与菜单管理：角色CRUD、菜单树CRUD（目录/页面/按钮三类）、角色菜单批量授权、权限标识字典管理。前端角色管理（含授权弹窗）与菜单管理页面。验收：新建测试角色仅授权部门菜单，绑定测试用户后登录只见部门菜单，越权调用用户接口返回403并记录日志。

### M2 业务模块（建议3天）

T1 职位管理：sys_position CRUD + 绑定角色；员工表增加position_id，员工管理页面选择职位。验收：不同职位薪资与描述可维护，职位绑定角色后新用户选该职位自动获得对应权限模板。

T2 考勤管理：Excel导入（模板列：员工账号、日期、签到、签退、状态、地点；逐行校验，错误行报告）、记录分页查询（按员工/部门/月份筛选）、手动补录、att_rule规则维护页。验收：导入样例表成功，同员工同日重复导入覆盖，错误行提示明确。

T3 薪资管理：手动奖惩录入；按月生成工资单（base+考勤+奖惩汇总，职位或考勤变动时草稿可重算，已确认锁定）；工资单分页查询与确认发放；明细页展示三项构成。验收：构造一名员工（迟到2次+旷工1天+奖励100），生成工资单数值与规则计算一致。

T4 个人中心：个人信息查看修改、修改密码（校验旧密码）、我的考勤页（按月列表）、我的薪资页（按月工资单及明细）。验收：普通员工登录仅见个人中心与被授权菜单，考勤薪资仅能看本人数据。

### M3 RAG知识库一期（建议2~3天）

按设计方案十一章执行：T1 三张kb表迁移+知识库/文件管理接口（含软删、重建）；T2 解析入库异步任务（txt/md/pdf/docx，切分参数500/80，清洗，智谱embedding-3向量化，Chroma一库一collection，双写MySQL）；T3 检索问答接口（多轮改写、top_k=6、SSE流式message/citations/done事件）与检索调试接口；T4 前端知识库管理页（库列表、文件上传与状态、切片预览）与问答调试页（流式渲染+引用来源展示）。验收：上传一份PDF员工手册后提问"迟到怎么处理"，答案基于文档内容并带文件名/标题路径/页码引用；软删该文件后同样问题不再引用该文件。

### M4 AI智能中心（建议3~4天）

T1 模型配置：ai_model CRUD（api_key Fernet加密）、默认模型切换、连通性测试按钮。前端模型配置页对齐实训要求截图风格。

T2 NL2SQL：product数据录入页与CRUD；自然语言→SQL生成（prompt限定仅SELECT、仅product表、强制LIMIT 100）；待审核列表、通过/驳回；通过后执行（只读连接执行，超时5秒），结果与耗时写入nl2sql_record；查询历史页（原句、SQL、时间、结果）。验收：输入"库存大于100的产品按价格倒序"生成正确SQL，未审核不可执行，非SELECT语句直接拒绝。

T3 AI助手（LangGraph）：MiMo-V2.5经OpenAI兼容接口接入；LangGraph图：agent节点（工具决策）⇄tools节点（retrieve知识库检索、nl2sql产品查询）→generate（SSE流式）；会话与消息持久化；多轮上下文。前端AI助手对话页（类豆包布局、会话列表、流式输出、引用展示）。验收：同一会话中先问知识库问题（引用文档）再问产品数据（走NL2SQL工具），均正确作答；深度思考模式回传assistant消息携带reasoning_content。

### M5 完善与交付（建议1~2天）

T1 工作台数据看板：用户、部门、职位、考勤异常、工资单等统计卡片与图表。
T2 全量回归：按"七、测试与交付检查清单"逐项过；修复问题。
T3 交付物：README（启动步骤、预置账号、目录说明）、.env.example、数据库初始化脚本或说明；整理git提交历史。

## 七、测试与交付检查清单

1. 认证：正确账号登录、错误密码限流锁定、Token过期刷新、退出后接口401。
2. RBAC：动态菜单差异化展示、无权限按钮隐藏、越权接口403并记录审计日志。
3. 组织管理：员工/部门/职位/角色/菜单全部增删改查、启停、排序正常；删除均为软删除且列表不再显示。
4. 考勤薪资：导入、规则修改、工资单三项构成数值正确；普通员工仅见个人数据。
5. RAG：上传→解析→入库状态流转；重复上传去重；软删后检索不可见；问答带引用、无据时明确回答不知道；SSE流式正常。
6. NL2SQL：生成SQL正确性、审核流程强制、仅SELECT、历史完整。
7. AI助手：多轮对话、知识库与产品数据混合提问、流式输出、会话历史。
8. 通用：所有列表分页；接口文档/docs完整；无硬编码密钥；前端无控制台报错。

## 八、风险与注意事项

1. 进度管理：实训周期内tokens与时间有限，严格按里程碑推进，M1是全局地基优先保障；每任务小步提交，避免大规模返工。
2. 外部API依赖：MiMo与智谱API不可用时，AI功能退化为错误提示而非崩溃；Embedding失败时文件状态标记失败可重试。
3. 数据安全：api_key加密存储、密码bcrypt、SQL执行仅只读账号且白名单表、上传白名单+魔数校验。
4. 换机部署：数据库utf8mb4、media与chroma目录随项目迁移、静态资源配置相对路径。
5. 遗留决策：会话表归属AI中心（本文档5.4已定义）；RAG二期（混合检索、重排、CRAG）与AI助手服务器管理MCP能力为选做加分项，排期允许再做。
