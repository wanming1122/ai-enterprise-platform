# 组织架构模块设计方案

版本：v4\.1

更新时间：2026\-09\-04

对齐技术栈：前端 React 18 \+ TypeScript，后端 FastAPI，数据库 MySQL，AI Agent 框架 LangGraph，RAG 向量数据库 Chroma

状态：技术栈迁移设计版（由 Vue3 \+ Django 方案迁移至 React \+ FastAPI 方案，含RAG知识库模块完整设计）

适用范围：组织管理、权限管理、用户生命周期、注册审批、入职邀请、个人中心及相关审计能力，以及RAG知识库模块（知识库管理、文件解析与切片入库、向量化检索、多模态理解、引用溯源）

## 一、模块定位

组织架构模块是企业管理系统的基础模块，负责统一维护企业部门、用户、角色、菜单、权限、登录认证、注册审批、入职邀请和操作审计。当前系统中的考勤管理、薪资管理、AI 智能中心、RAG 知识库、系统设置等业务模块，均依赖组织架构模块提供的用户身份、部门归属、角色权限和菜单授权能力。

当前模块已从早期的单纯组织架构管理，扩展为企业基础平台能力，全面支撑系统基础运行。模块支持自定义用户体系管理，可维护用户账号、姓名、联系方式、岗位、头像等个人信息；搭建多级企业部门组织树，支持部门负责人配置、排序与启停管理；搭载完整的RBAC权限体系，实现角色、菜单、按钮级别的精细化权限管控；支持登录后动态生成用户专属菜单路由。

同时模块具备完善的用户准入机制，支持普通员工自主注册、特殊身份注册审批、管理员定向入职邀请注册；配套完善的密码安全防护机制，包含登录限流、验证码找回密码、强密码校验、密码重置功能。全程记录系统关键操作行为，实现操作审计溯源，同时提供完整的个人中心能力，支持用户自主维护个人信息、密码、查看考勤薪资数据。

## 二、技术架构

### 2\.1 当前技术栈

前端整体采用React 18搭配TypeScript开发，使用函数组件与Hooks编写业务逻辑，界面基于Ant Design组件库搭建，适配系统各类表单、弹窗、菜单、表格等基础组件。前端状态管理依托Zustand，专门存储用户信息、菜单权限、账号状态等全局数据。路由采用React Router动态路由机制，根据后端返回的用户权限菜单树，动态注册页面路由，实现不同角色用户差异化页面访问效果。

后端核心框架为FastAPI，组织架构相关业务代码统一存放于后端app/org目录，按routers、schemas、models、services分层组织。系统认证体系基于PyJWT实现，登录后生成有效期可控的访问令牌与刷新令牌，通过FastAPI依赖注入体系完成用户身份鉴权。数据库统一使用MySQL，ORM层采用SQLAlchemy，数据表结构变更通过Alembic迁移脚本管理，开发与正式环境统一使用MySQL，保障数据稳定性。

系统接口文档由FastAPI内置OpenAPI机制自动生成，可通过/docs（Swagger UI）与/redoc路径在线查看。系统图片、用户头像等静态资源不依赖本地文件路径，经过压缩处理后转换为Data URL格式，直接存储在数据库字段中，解决换电脑部署资源丢失问题。AI相关能力（AI助手、NL2SQL、RAG知识库）与组织架构核心业务统一由FastAPI服务承载，AI Agent整体采用LangGraph框架编排，RAG检索采用Chroma向量数据库（详见"十一、RAG知识库模块设计"）。

### 2\.2 后端分层架构

后端采用标准化分层开发架构，请求从入口逐层处理，层级清晰、职责独立。首先通过全局路由配置与模块专属路由（APIRouter）接收前端请求，经由路由处理函数与依赖注入完成请求参数处理与权限校验，再通过Pydantic模型完成数据参数校验、格式转换与返回数据封装。核心业务逻辑统一封装在业务服务层，分离路由与业务代码，最后通过SQLAlchemy ORM模型操作MySQL数据库，完成数据读写。

后端核心源码文件分工明确，models\.py统一定义数据库实体模型，构建系统所有数据表结构；routers\.py实现各类接口路由，接收并响应前端请求；schemas\.py基于Pydantic负责数据序列化与参数校验；main\.py统一注册应用与全局路由；dependencies\.py封装认证与核心权限校验逻辑。

同时拆分独立业务服务文件，user\_service\.py处理用户相关业务、department\_service\.py处理部门管理业务、menu\_service\.py管控菜单配置、role\_service\.py实现角色权限管理、invitation\_service\.py负责入职邀请全流程逻辑、operation\_log\_service\.py统一处理系统操作审计日志记录。

### 2\.3 前端分层架构

前端采用模块化分层架构，入口层级清晰。api层统一封装所有后端接口请求，统一请求格式与错误处理；types层定义所有接口参数与返回数据类型，适配TypeScript语法规范；stores全局状态层基于Zustand存储用户登录信息、权限列表、菜单树等全局数据。

router路由层区分静态基础路由与用户专属动态路由，实现权限差异化路由加载；components权限组件层封装HasPermission权限组件与usePermission权限Hooks，控制页面按钮显隐；views视图层存放所有组织架构相关业务页面，包含登录注册、用户管理、部门管理、角色菜单、审批邀请、日志与个人中心等页面。

前端核心页面各司其职，登录注册页面处理用户账号登录、自助注册、密码找回功能；用户管理页面实现员工账号全生命周期管理；注册审批页面处理特殊身份用户的注册审核流程；部门、角色、菜单页面分别完成组织架构与权限体系的维护；入职邀请页面支持管理员定向邀请外部人员入职；日志页面提供全量操作审计记录查询；个人中心支持用户自主管理个人信息、密码、考勤薪资数据。动态路由与权限指令联动，实现页面与按钮的双层权限控制。

## 三、功能结构

### 3\.1 当前菜单结构

系统左侧核心菜单包含工作台、组织管理、操作日志、系统配置、个人中心五大核心板块。其中组织管理为核心业务模块，包含用户管理、注册审批、部门管理、角色管理、菜单管理、入职邀请管理六大子功能。个人中心下设我的信息、我的工资、我的考勤三个个人功能页面。

系统菜单展示遵循权限控制规则，页面真实展示内容由后端根据当前登录用户角色动态返回菜单树决定。超级管理员拥有系统全部菜单与按钮权限，可操作所有功能；普通用户仅展示所属角色授权的菜单页面，无权限功能自动隐藏。页面按钮权限通过菜单类型与专属权限标识精准控制，实现精细化权限管控。

### 3\.2 模块核心职责

用户管理模块主要实现系统员工账号的全生命周期管理，支持用户新增、编辑、删除、账号启停、密码重置，同时具备批量注册、Excel批量导入导出员工数据、查看员工档案等能力，完整支撑企业员工信息管理需求。

部门管理模块负责企业组织架构树维护，支持多级部门创建、上级部门绑定、部门负责人配置、部门状态启停与自定义排序，搭建完整的企业组织层级体系。

角色管理模块用于系统角色的创建与维护，核心能力为角色基础信息配置、菜单权限授权、页面按钮权限分配，通过角色为载体，批量为用户分配系统操作权限。

菜单管理模块管控系统所有页面菜单与功能按钮，支持目录、页面菜单、功能按钮三类节点维护，可配置路由路径、前端组件、图标、权限标识、排序与启停状态，是动态权限体系的核心基础。

注册审批模块专门处理非普通员工的注册申请，支持管理员查看注册申请详情、审核通过、驳回申请，规范特殊身份用户的入驻流程，保障系统账号准入安全。

入职邀请模块面向企业外部人员入职场景，管理员可创建专属入职邀请、生成邀请链接、重发、撤销、删除邀请，全程记录邀请状态日志，外部人员可通过邀请链接完成注册入职并自动绑定预设部门与角色。

操作日志模块统一记录系统所有关键操作行为，支持日志条件查询、查看请求参数、操作时间、操作IP，可精准定位用户操作行为与系统异常问题，满足审计需求。

个人中心模块面向所有登录用户，支持自主修改个人资料、上传头像、修改登录密码、查看安全登录记录，同时可查看个人薪资与考勤数据，满足用户个人信息管理与数据查看需求。

## 四、数据库设计

### 4\.1 核心实体关系说明

系统数据库以部门、用户、角色、菜单为核心实体，构建完整的组织与权限关联体系。部门支持多级父子嵌套结构，每个部门可绑定多个系统用户；用户与角色为多对多关联关系，一个用户可绑定多个角色，一个角色可分配给多个用户；角色通过授权关联对应菜单，菜单可嵌套父子目录结构，同时菜单绑定专属权限标识，实现按钮级权限控制。

系统所有用户操作行为关联操作日志记录，用户注册申请关联审批记录，入职邀请关联对应部门、角色与邀请日志，所有实体关联关系闭环，保障组织、权限、审批、邀请、审计全流程数据联动。

### 4\.2 组织与权限核心数据表

sys\_user用户表用于存储所有系统员工账号信息，包含登录账号、密码、个人资料、联系方式、部门岗位、头像、账号状态、登录记录等核心数据，是系统用户体系的基础数据表。

sys\_department部门表用于存储企业部门架构数据，支持多级部门层级、部门负责人、排序、启停状态等配置，搭建企业组织树。

sys\_role角色表用于定义系统所有权限角色，区分超级管理员、普通管理员、普通员工等不同身份角色。

sys\_user\_role\_relation用户角色关联表，实现用户与角色的多对多绑定关系，支持一人多角色配置。

sys\_menu菜单表存储系统所有目录、页面、按钮菜单数据，包含路由、组件、权限标识、显隐状态等配置，支撑动态菜单与路由功能。

sys\_role\_menu\_relation角色菜单关联表，记录角色对应的授权菜单，实现角色菜单权限分配。

sys\_permission权限表存储系统所有功能权限标识字典，统一管理权限编码。

sys\_menu\_permission\_relation菜单权限关联表，实现菜单节点与权限标识的绑定，支撑按钮级权限控制。

### 4\.3 注册、邀请与安全数据表

sys\_registration\_approval注册审批表，存储非普通员工的注册申请记录、审批状态、审批意见、申请人信息等数据。

sys\_invitation入职邀请表，记录管理员创建的所有入职邀请信息，包含预设部门、角色、有效期、邀请状态等。

sys\_invitation\_log邀请日志表，全程记录邀请创建、打开、注册、重发、撤销、过期等所有状态变更记录。

sys\_password\_recovery\_code密码找回表，存储用户找回密码的验证码哈希值、尝试次数、过期时间，保障密码找回安全。

sys\_log操作日志表，记录系统所有关键接口操作、权限拦截、异常操作等审计数据。

sys\_config系统配置表，存储系统品牌、图标、主题等全局配置数据，图片资源以Data URL格式存储。

sys\_dict\_type字典类型表与sys\_dict\_item字典项表，为系统所有业务模块提供状态、标签等基础字典数据支撑。

### 4\.4 用户表核心字段说明

用户表核心字段包含唯一登录账号、bcrypt（Passlib）哈希加密密码、用户昵称、真实姓名、性别、生日等基础资料，同时绑定邮箱、手机号、社交账号等联系方式，用于身份验证与密码找回。关联部门ID与岗位字段，标识用户所属组织与岗位；头像字段以Data URL格式存储图片数据，无需依赖本地文件。

系统支持账号启停状态控制，可标记用户是否需要重置密码，同时存储用户个性化配置，包含主题模式、菜单布局、消息提醒、默认首页等。自动记录用户最近登录IP与登录时间，留存登录溯源信息。

### 4\.5 菜单表核心字段说明

菜单表支持父子菜单层级配置，通过父菜单ID实现目录树结构；包含菜单名称、前端路由路径、组件映射地址、图标样式等页面展示字段。区分目录、页面菜单、功能按钮三种菜单类型，支持设置菜单是否可见、是否为外链、启停状态。

核心权限字段为权限标识，用于前端按钮控制与后端接口鉴权，同时支持自定义排序序号，实现菜单页面自定义展示顺序。

## 五、接口设计

### 5\.1 接口统一规范

组织架构模块所有接口采用统一返回格式，包含状态码、提示信息与业务数据，分页接口单独封装分页参数，返回数据列表、总条数、当前页码、每页条数。系统所有需要鉴权的接口，统一采用Bearer令牌认证方式，通过请求头携带登录Token完成身份校验。

### 5\.2 认证接口

系统提供完整的账号认证接口体系，包含账号密码登录接口，登录成功后返回双Token、用户信息、菜单树与权限列表；支持令牌刷新接口，实现无感知续登。对外开放注册配置查询接口，获取系统允许自助注册的角色类型；提供用户自助注册接口，支持普通员工直接注册、特殊角色提交审批。

配套密码找回接口体系，支持发送验证码、校验验证码、重置密码，全程做限流与过期校验，保障账号安全。

### 5\.3 用户接口

用户接口覆盖用户全生命周期管理，支持用户列表查询、新增、编辑、删除、账号启停、密码重置。提供员工档案查询接口，展示用户完整入职与个人资料。支持批量注册用户、Excel批量导入导出用户数据，提供导入模板下载功能，适配企业批量人员录入场景。

针对个人中心场景，提供当前用户信息查询、个人资料修改、头像上传、密码修改、登录设备安全概览查询等专属接口，仅登录用户可调用。所有管理类接口均绑定对应权限标识，无权限用户调用会触发403权限拦截。

### 5\.4 部门、角色、菜单和权限接口

部门接口支持部门列表查询、树形结构获取、部门启停、自定义排序、增删改查，所有操作绑定对应部门权限标识。角色接口实现角色维护、菜单批量授权功能，支持为角色分配目录、页面、按钮全量权限。

菜单接口支持菜单树查询、菜单启停、排序、增删改查，完整维护系统权限节点。权限接口支持系统权限标识字典的全量管理，支撑全模块权限配置。

### 5\.5 注册审批接口

注册审批接口提供审批列表、审批详情查询能力，管理员可通过接口完成注册申请通过、驳回操作。审批完成后自动更新用户账号状态、绑定对应角色，同步记录审批日志与系统操作审计日志，全程可追溯。

### 5\.6 入职邀请接口

管理员端支持邀请列表查询、创建邀请、查看邀请详情、重发邀请、撤销邀请、删除邀请，全程记录邀请状态变更。对外开放公开接口，支持外部用户校验邀请令牌、通过邀请链接完成注册入驻，无需登录系统即可完成入职流程。

### 5\.7 日志和配置接口

操作日志接口支持全量日志查询，仅开放只读权限，保障审计数据不可篡改。系统配置接口支持全局配置项维护、系统主题与品牌设置、图片资源上传压缩转换。公共配置接口为登录页面提供系统名称、图标等公共展示数据。工作台统计接口可统计用户、部门、角色、审批、邀请等全量基础数据，支撑首页数据看板展示。

## 六、权限设计

### 6\.1 RBAC权限模型

系统采用标准RBAC权限模型，以用户、角色、菜单、权限标识为核心构建权限体系。用户通过绑定角色获取权限，角色关联系统菜单，菜单绑定专属权限标识，实现从账号到功能按钮的全链路权限管控。同时支持独立权限表绑定机制，菜单可关联多条权限标识，适配复杂业务权限场景。

### 6\.2 后端权限判断规则

后端权限校验为系统最终鉴权依据，优先级最高。接口视图提前配置所需权限标识，超级管理员账号默认豁免所有权限校验，拥有系统全部操作权限。普通用户登录后，系统自动汇总其绑定所有角色的菜单与权限标识，校验当前操作是否具备对应权限。无权限访问时，系统自动返回403权限拦截状态码，同时自动记录权限拦截审计日志，留存操作记录。

### 6\.3 前端权限控制

前端权限主要用于优化用户体验，辅助后端实现权限管控。用户登录后，前端全局存储用户资料、授权菜单树、权限标识数组。通过动态路由机制，仅加载用户授权菜单对应的路由，隐藏无权限页面；页面菜单根据权限数据动态渲染，不展示无权限菜单。

所有功能按钮通过自定义权限指令控制显隐，无对应权限则自动隐藏按钮，避免用户误操作。所有前端权限控制仅为页面展示优化，最终接口权限仍由后端严格校验，防止前端权限绕过风险。

### 6\.4 典型权限码说明

系统为各模块分配标准化权限码，用户模块包含列表查询、新增、编辑、删除、账号启停、密码重置、导入导出等权限标识；部门模块包含部门查询、新增、编辑、删除、启停、排序权限；角色模块包含角色维护、菜单授权权限；菜单模块包含菜单维护、启停排序权限。

注册审批模块包含审批查询、通过、驳回权限；入职邀请模块包含邀请新增、查询、重发、撤销、删除权限；个人中心模块包含个人资料、薪资查看、考勤查看专属权限，全面覆盖所有系统操作场景。

## 七、核心业务流程

### 7\.1 登录流程

用户在前端登录页面输入账号密码，提交登录请求至后端登录接口。后端校验账号密码合法性、账号启停状态，校验通过后，更新用户最近登录时间与登录IP，记录登录日志。同时生成双Token令牌、汇总用户信息、授权菜单树与权限列表，统一返回前端。

前端接收数据后，全局存储令牌与用户权限信息，根据后端返回的菜单树动态注册页面路由，完成登录跳转。系统具备完善的登录安全机制，密码错误会累计失败次数，账号或IP多次登录失败会触发临时锁定，锁定时长可配置，有效防范暴力破解。

### 7\.2 自助注册与审批流程

用户进入注册页面，选择对应身份角色，填写账号、密码、个人资料与联系方式，提交注册申请。系统自动判断用户注册角色类型，普通员工角色可自动审核通过，账号直接启用并完成注册。

非普通员工角色注册后，账号默认禁用，自动生成注册审批记录，推送审批通知至管理员。管理员在注册审批页面查看申请详情，可选择通过或驳回审批。审批通过后，自动启用用户账号并绑定申请角色；驳回审批则保持账号禁用状态，记录驳回原因与操作日志。

系统严格限制高危角色自助注册，超级管理员、用户管理员身份不支持自助注册。注册账号必须绑定至少一种联系方式，密码需满足八位以上、字母数字组合的强密码规则，保障注册账号安全性。

### 7\.3 入职邀请流程

管理员在入职邀请管理页面，填写被邀请人姓名、联系方式、预设部门、角色、岗位与邀请有效期，创建专属入职邀请，系统自动生成唯一邀请令牌与邀请链接。邀请创建后进入待发送或已发送状态，全程记录状态日志。

外部候选人打开邀请链接，系统校验令牌有效性与有效期，校验通过后进入注册页面，填写账号密码完成注册。注册成功后，系统自动创建用户账号，绑定预设部门与角色，邀请状态更新为已注册。管理员可随时对未过期、未注册的邀请进行重发、撤销、删除操作，所有状态变更均留存日志记录。

### 7\.4 角色授权流程

管理员在角色管理页面，完成角色基础信息的新增或编辑后，打开菜单授权弹窗。系统加载完整系统菜单树，管理员可勾选需要授权的目录、页面、功能按钮，提交保存后，授权关系同步存入数据库。用户下次登录或刷新权限信息后，新的授权规则自动生效，更新用户可访问菜单与操作权限。

### 7\.5 图片和头像存储流程

系统彻底解决换电脑部署图片丢失问题，所有头像、系统图标、登录页图片等资源上传后，后端首先校验文件格式与大小，拦截SVG等存在脚本注入风险的文件。对合规图片进行压缩处理，统一尺寸与画质，随后转换为Data URL格式，直接存储至数据库对应字段。所有图片资源不依赖本地文件路径，迁移部署后可正常展示。

## 八、前端页面设计

### 8\.1 用户管理页面

用户管理页面支持多条件筛选、分页查询系统用户列表，展示用户姓名、账号、部门、岗位、状态、登录时间等信息。管理员可手动新增、编辑、删除用户，为用户分配部门与角色，启停账号状态、重置登录密码。

页面支持批量操作，可批量注册账号、Excel批量导入员工数据、导出用户信息，同时提供导入模板下载功能。支持查看员工完整档案信息，所有操作按钮均绑定权限指令，无权限用户无法查看与操作，导出功能做数据脱敏处理，保护员工隐私。

### 8\.2 部门管理页面

部门管理页面以树形结构展示企业完整组织架构，层级清晰、一目了然。支持新增下级部门、编辑部门信息、删除空部门，可配置部门负责人、联系电话、邮箱、排序序号与启停状态。

系统具备严格的业务约束，禁止设置循环父子部门，存在子部门的父部门无法删除，部门下绑定员工的部门无法删除，避免组织架构数据错乱。支持拖拽排序与批量排序，灵活调整部门展示顺序。

### 8\.3 角色管理页面

角色管理页面支持角色列表查询、新增、编辑、删除，角色名称、角色编码全局唯一，角色编码仅支持字母开头格式。核心功能为菜单权限授权，管理员可批量为角色分配目录、页面、按钮权限，灵活配置不同岗位用户的操作范围。禁用角色后，该角色对应的所有用户自动失去对应权限。

### 8\.4 菜单管理页面

菜单管理页面以树形结构展示系统所有功能节点，区分目录、页面菜单、功能按钮三类节点。支持新增、编辑、删除菜单，配置路由路径、前端组件、图标、权限标识、排序、显隐与启停状态。

业务约束严格，目录与页面必须配置路由路径，功能按钮必须绑定专属权限标识，系统自动校验，禁止生成循环父子菜单，保障菜单架构规范有序。

### 8\.5 注册审批页面

注册审批页面默认展示所有待审批注册申请，支持按账号、姓名、联系方式、申请角色多条件筛选查询。可查看每条注册申请的详细资料、申请时间、申请身份，管理员可对未处理申请执行通过或驳回操作，并填写审批意见。

审批通过后自动启用用户账号、绑定对应角色，同步记录审批日志与系统操作日志；驳回申请则保留用户禁用状态，记录驳回原因，全程可追溯审批流程。

### 8\.6 入职邀请管理页面

入职邀请页面支持管理员创建入职邀请，完整填写被邀请人信息、所属部门、预设角色、岗位、邀请有效期等信息，生成专属邀请链接。页面展示所有邀请记录与对应状态，支持查看邀请详情、复制邀请链接、重发邀请、撤销未过期邀请、删除无效邀请。

系统精准区分邀请状态，包含待发送、已发送、已打开、已注册、已过期、已撤销、处理失败等多种状态，状态变更全程留痕。外部人员通过公开链接完成注册后，自动更新邀请状态，无需管理员手动处理。

## 九、安全设计

### 9\.1 认证与会话安全

系统基于PyJWT实现无状态令牌认证，通过FastAPI依赖注入统一拦截校验，访问令牌短期有效，默认三十分钟过期，保障账号安全；刷新令牌有效期更长，默认七天，支持用户无感知续登。系统开启刷新令牌轮换与黑名单机制，过期令牌、作废令牌自动拦截，防止令牌复用风险。用户登录后，系统返回完整的身份、权限、菜单数据，保障单次登录即可获取全部系统权限资源。

### 9\.2 密码安全策略

系统强制启用高强度密码策略，用户密码最小长度为八位，必须同时包含字母与数字组合，杜绝简单弱密码。所有用户密码通过bcrypt（Passlib）算法哈希存储，数据库无明文密码，杜绝密码泄露风险。

管理员重置用户密码后，系统自动标记账号为需重置密码状态，用户首次登录必须修改密码。密码找回验证码仅存储哈希值，不保存明文验证码，有效防止验证码泄露。

### 9\.3 找回密码限流机制

系统对密码找回功能做严格限流防护，验证码有效期固定为十分钟，单账号十五分钟内仅限三次验证码发送，单IP十五分钟内仅限十次发送，防止恶意刷取验证码。同时限制验证码最多尝试五次，多次错误后锁定找回权限，有效防范暴力破解与恶意攻击。

### 9\.4 登录限流机制

登录接口具备完善的限流锁定能力，单账号连续五次登录失败，自动锁定账号十五分钟；单IP连续二十次登录失败，同样锁定十五分钟，有效拦截暴力破解、批量扫号等恶意攻击行为，保障系统账号安全。

### 9\.5 操作审计日志

系统通过统一审计日志混入类，自动记录所有登录用户的关键接口操作，完整留存操作用户、操作模块、操作类型、请求地址、请求方式、请求参数、操作IP、设备信息、响应状态、错误信息、接口耗时等全维度数据。

针对登录失败、权限拦截、注册审批、密码修改、邀请状态变更等敏感操作，系统单独标记记录，方便后续安全审计与问题溯源，所有日志数据仅可查询、不可修改删除，保障审计真实性。

## 十、与其他模块的关联关系

组织架构模块是全系统的基础支撑模块，所有业务模块均依赖其身份与权限体系运行。工作台模块的统计数据，全部来源于组织架构模块的用户、部门、角色、审批、邀请、日志等真实数据。

考勤管理、薪资管理模块以组织架构的用户、部门数据为基础，搭建员工考勤与薪资体系，普通用户仅可查看个人考勤薪资数据，权限由组织模块统一管控。

AI智能中心、RAG知识库模块的页面访问、功能操作权限，完全依托组织模块的菜单与按钮权限体系，用户操作记录归属对应登录账号。其中AI智能中心的Agent能力基于LangGraph框架编排，RAG知识库采用Chroma向量数据库承载语义检索，详细设计见"十一、RAG知识库模块设计"。

系统设置、字典管理模块为组织架构及所有业务模块提供全局配置与基础字典数据，系统品牌、主题、图标等配置统一存储在组织模块的系统配置表中，为全系统提供展示配置支撑。

## 十一、RAG知识库模块设计

### 11\.1 模块定位

RAG知识库模块是AI智能中心的知识底座，负责企业内部文档（员工手册、考勤制度、报销流程、产品资料等）的入库、切分、向量化与检索，为AI助手提供可溯源的知识问答能力。用户上传文档后，系统自动完成解析、切分与向量化入库；AI助手回答问题时检索相关知识片段，答案附带文件名、标题路径、页码等引用信息，做到有据可查、无据不答。

模块页面与操作权限完全依托组织架构模块的RBAC体系，菜单挂载于AI智能中心之下。文件删除遵循系统全局规则，前端删除一律软删除，数据不做物理清除。生成模型与Embedding模型彻底解耦，模型信息统一由模型配置页面管理，与NL2SQL等AI能力共用配置体系。

### 11\.2 技术架构与数据流

模块技术选型如下表所示：

| 层次 | 选型 | 说明 |
| --- | --- | --- |
| 后端框架 | FastAPI | 与组织架构业务同服务承载，异步接口原生支持 |
| 结构化存储 | MySQL \+ SQLAlchemy \+ Alembic | 知识库、文件、切片的业务数据与元数据 |
| 向量数据库 | Chroma（PersistentClient本地持久化） | 仅存向量与检索必需数据 |
| Agent编排 | LangGraph | 检索能力以工具形式注册进Agent |
| 生成模型 | MiMo-V2.5 | 原生全模态、强工具调用、1M上下文，百炼API或自部署接入 |
| Embedding | 智谱embedding-3 / 通义text-embedding-v3（在线默认），BAAI/bge-m3（本地备选） | 可配置，维度创建时锁定 |
| 重排（二期） | bge-reranker-v2-m3（本地）或在线rerank API | 粗排转精排 |
| 文档解析 | pdfplumber、python-docx、pandas、RapidOCR（可选） | 覆盖PDF、Word、Excel、Markdown、TXT、图片 |

入库链路：文件上传后走异步任务处理，接口立即返回，前端通过状态接口查看进度，避免大文件解析超时。整体链路为上传→格式校验→解析→清洗→切分→Embedding→双写存储→状态回写。格式校验包含后缀白名单、文件头魔数校验与大小上限，并通过SHA-256哈希做同库去重；清洗负责去除页眉页脚、多余空行与乱码字符，表格还原为Markdown表格；双写将切片内容与元数据写入MySQL，向量与必要metadata写入Chroma对应collection；文件状态机为待解析→解析中→已入库/失败，失败记录原因并支持重新解析，全流程关键节点写操作审计日志。

查询链路：用户问题→多轮查询改写→Embedding→Chroma向量检索（metadata过滤软删）→（二期混合检索与重排）→组装上下文→MiMo-V2.5流式生成→答案与引用溯源一并返回。FastAPI使用StreamingResponse以SSE方式推送生成内容，引用列表在生成结束后作为独立事件下发。

解耦原则：生成模型只消费检索返回的纯文本切片，与Embedding无任何协议或厂商耦合，切换生成模型不影响向量库；Chroma collection维度在创建时固定，更换Embedding模型必须新建collection并从MySQL切片表全量重嵌入，模块提供按库重建索引接口。知识库表记录入库时的模型名与维度，模型配置页更换默认Embedding模型仅影响新建知识库，存量知识库提示重建。

部署形态：MiMo-V2.5经百炼API接入时，Embedding同样走在线API，全程无需本地GPU，实训演示环境推荐；自部署MiMo-V2.5（MIT开源）时，Embedding可选本地bge-m3同机部署，数据全程内网闭环。

### 11\.3 文件解析与多模态设计

各格式解析方案如下：

| 格式 | 解析方案 | 说明 |
| --- | --- | --- |
| PDF | pdfplumber | 文本与表格抽取，扫描件OCR（RapidOCR）为可选增强 |
| Word（docx） | python-docx | 段落与表格 |
| Markdown / TXT | 直接读取 | Markdown额外走标题切分 |
| Excel / CSV | pandas | 表格按行组切分 |
| 图片（png/jpg） | MiMo-V2.5生成描述 | 描述文本走正常文本链路 |

多模态处理复用主模型：图片上传后调用MiMo-V2.5的原生全模态能力生成内容描述，描述文本再走正常文本切分与向量化链路入库，不额外引入独立视觉模型。此类切片chunk_type标记为image_description，metadata保留原始图片的存储路径，前端引用溯源时可回显原图。

原始文件存储：知识库文件普遍为MB级二进制文档，不适合沿用组织模块小图片的Data URL入库方案，统一存储于服务端media目录（路径经.env配置），MySQL仅存相对路径与哈希，备份迁移时连同media目录一起处理。

### 11\.4 切分方案

通用文本采用RecursiveCharacterTextSplitter，分隔符优先级为\["\n\n", "\n", "。", "；", "，", " "\]，默认chunk_size=500字符、overlap=80。中文信息密度高，500字符可在主流Embedding模型512 token窗口内保持语义完整，重叠部分保证跨片语义连续；切分参数为知识库级配置，存于知识库表，可按库调参。

结构化文档方面，Markdown先按标题层级切分，标题路径（如"员工手册>考勤制度>迟到处理"）写入切片metadata，检索命中后来源展示清晰；表格类内容小表整表作为一个切片，大表按20~30行分组切分且每组重复携带表头，chunk_type标记为table。每个切片必带metadata：kb_id、file_id、chunk_id、chunk_index、title_path、page、chunk_type、status，支撑检索过滤、引用溯源、按文件删除与按库重建索引。

### 11\.5 Embedding与Chroma组织

Embedding在线默认智谱embedding-3或通义text-embedding-v3，本地备选BAAI/bge-m3（1024维），经模型配置页面统一管理，知识库创建时锁定所用模型标识与维度并记录入库。

Chroma采用一个知识库一个collection的组织方式，命名规则kb_{id}，整库删除即delete_collection，权限天然隔离，且规避不同维度向量混用问题；检索统一附加where=\{"status": "active"\}过滤软删数据。软删除映射上，MySQL侧status为TINYINT（1有效、2软删除），Chroma侧metadata使用字符串status（active/deleted），删除文件时双侧同步标记、检索即刻不可见，恢复时双侧同步恢复，物理清理仅管理员后台按库执行。

### 11\.6 检索方案（分三期）

一期为基础检索链路：先将用户问题与最近三轮对话历史交由LLM重写为独立完整的检索query，解决多轮对话中的指代问题；随后向量检索top_k=6并做metadata过滤；组装prompt时约束模型仅根据参考资料回答、资料中没有就明确说不知道、答案标注\[1\]\[2\]引用；引用溯源返回文件名、标题路径、页码。

二期为混合检索与重排：向量检索叠加BM25关键词检索（jieba分词），RRF融合排序，对含精确标识符的查询（工号、单号、编号）提升明显；粗排top_k=20，经bge-reranker-v2-m3（本地）或在线rerank API精排取top 4~8，低于分数阈值的结果直接丢弃，宁少勿滥。

三期为自纠错检索（CRAG简化版）：LLM评估检索结果相关性，不满足则重写查询再检索一轮，最多两轮。依托MiMo-V2.5的1M上下文，top_k与上下文预算可放宽，但仍以重排保证精度，避免堆料稀释重点、增加成本。

### 11\.7 LangGraph集成设计

检索能力注册为retrieve工具，与NL2SQL等工具并列，由Agent自主决策是否检索，不做成固定死流程。图结构为agent节点（工具调用决策）与tools节点（retrieve、nl2sql等）循环，最终进入generate节点以SSE流式输出并附引用；前端AI助手页面与知识库问答共用此Agent，知识库检索只是其能力之一。

集成注意事项：接入前确认所选渠道（百炼或自部署推理服务）暴露OpenAI兼容的tools接口；深度思考模式下多轮带工具调用的会话，回传assistant消息必须完整携带reasoning_content，否则接口报错。

### 11\.8 数据库表设计

kb_knowledge_base知识库表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(64) | NOT NULL, UNIQUE | 知识库名称 |
| description | VARCHAR(255) | 可空 | 知识库描述 |
| embedding_model | VARCHAR(64) | NOT NULL | 入库使用的Embedding模型标识 |
| embedding_dimension | INT | NOT NULL | 向量维度，创建后不可变更 |
| chunk_size | INT | DEFAULT 500 | 切片长度（字符） |
| chunk_overlap | INT | DEFAULT 80 | 相邻切片重叠字符数 |
| collection_name | VARCHAR(64) | NOT NULL | Chroma collection名，规则kb_{id} |
| status | TINYINT | DEFAULT 1 | 1启用 0停用 2软删除 |
| creator_id | BIGINT | NOT NULL | 创建人，关联sys_user.id |
| created_at | DATETIME | | 创建时间 |
| updated_at | DATETIME | | 更新时间 |

kb_file文件表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| kb_id | BIGINT | NOT NULL | 所属知识库 |
| file_name | VARCHAR(255) | NOT NULL | 原始文件名 |
| file_type | VARCHAR(16) | NOT NULL | pdf/docx/md/txt/xlsx/csv/png/jpg |
| file_size | BIGINT | NOT NULL | 字节数 |
| content_hash | CHAR(64) | NOT NULL | SHA-256，同库去重 |
| storage_path | VARCHAR(255) | NOT NULL | media目录相对路径 |
| chunk_count | INT | DEFAULT 0 | 切片总数 |
| parse_status | TINYINT | DEFAULT 0 | 0待解析 1解析中 2已入库 3失败 |
| fail_reason | VARCHAR(255) | 可空 | 解析失败原因 |
| status | TINYINT | DEFAULT 1 | 1正常 2软删除 |
| uploader_id | BIGINT | NOT NULL | 上传人，关联sys_user.id |
| created_at | DATETIME | | 创建时间 |
| updated_at | DATETIME | | 更新时间 |

kb_chunk切片表：

| 字段名 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 切片ID，Chroma中的ids为其字符串形式 |
| file_id | BIGINT | NOT NULL | 所属文件 |
| kb_id | BIGINT | NOT NULL | 冗余字段，支撑按库重建索引 |
| chunk_index | INT | NOT NULL | 文件内切片序号 |
| content | TEXT | NOT NULL | 切片文本 |
| char_count | INT | | 切片字符数 |
| title_path | VARCHAR(255) | 可空 | 标题路径，如"员工手册>考勤制度>迟到处理" |
| page | INT | 可空 | 页码，PDF类文档生效 |
| chunk_type | VARCHAR(16) | DEFAULT 'text' | text/table/image_description |
| status | TINYINT | DEFAULT 1 | 1有效 2软删除 |
| created_at | DATETIME | | 创建时间 |

索引设计：kb_file(kb_id, content_hash)唯一索引实现同库哈希去重；kb_file(kb_id, status)支撑文件列表查询；kb_chunk(file_id)支撑切片预览与按文件删除；kb_chunk(kb_id, status)支撑按库重建与统计。creator_id、uploader_id关联sys_user，操作审计复用sys_log。切片内容在MySQL冗余存储一份，支撑管理界面切片预览与向量索引全量重建。会话与消息的持久化复用AI智能中心的会话体系，随AI中心模块设计统一对齐，本模块不重复建表。

### 11\.9 接口清单

接口统一前缀/api/v1/kb，Bearer令牌认证，返回格式\{code, message, data\}与本系统组织模块一致，分页参数page/page_size。

知识库管理接口：

| 方法 | 路径 | 说明 | 权限码 |
| --- | --- | --- | --- |
| GET | /api/v1/kb/bases | 知识库分页列表，支持名称关键词 | kb:list |
| POST | /api/v1/kb/bases | 新建知识库，锁定Embedding模型、维度与切分参数，并创建Chroma collection | kb:create |
| GET | /api/v1/kb/bases/{id} | 知识库详情，含文件数、切片数统计 | kb:list |
| PUT | /api/v1/kb/bases/{id} | 编辑名称、描述与切分参数（模型与维度不可改） | kb:update |
| DELETE | /api/v1/kb/bases/{id} | 软删除知识库及其下全部文件 | kb:delete |
| POST | /api/v1/kb/bases/{id}/rebuild | 重建向量索引，删除并重建collection后从kb_chunk全量重嵌入 | kb:update |

文件管理接口：

| 方法 | 路径 | 说明 | 权限码 |
| --- | --- | --- | --- |
| POST | /api/v1/kb/bases/{id}/files | multipart上传，类型、大小、魔数校验与哈希去重，异步解析入库 | file:upload |
| GET | /api/v1/kb/bases/{id}/files | 文件分页列表，支持类型与解析状态筛选 | file:list |
| GET | /api/v1/kb/files/{file_id} | 文件详情：解析状态、切片数、失败原因 | file:list |
| GET | /api/v1/kb/files/{file_id}/chunks | 切片分页预览：内容、标题路径、页码 | file:list |
| POST | /api/v1/kb/files/{file_id}/reparse | 重新解析入库，清空原切片后重跑 | file:reparse |
| DELETE | /api/v1/kb/files/{file_id} | 软删除文件，MySQL与Chroma双侧标记 | file:delete |

检索与问答接口：

| 方法 | 路径 | 说明 | 权限码 |
| --- | --- | --- | --- |
| POST | /api/v1/kb/search | 检索调试：query、kb_ids、top_k，返回切片、相似度分数与溯源信息 | kb:search |
| POST | /api/v1/kb/chat | 知识库问答，SSE流式：message事件推送增量、citations事件推送引用列表、done事件结束，带conversation_id支持多轮 | kb:chat |
| GET | /api/v1/kb/conversations/{id} | 会话历史查询 | kb:chat |

模型配置由AI智能中心的模型配置页面统一承载（生成、Embedding、Rerank三类，/api/v1/ai/models系列接口），本模块仅读取Embedding与Rerank配置，不重复建设。

### 11\.10 权限与安全

权限码包含kb:list（知识库查询）、kb:create（新建）、kb:update（编辑与重建索引）、kb:delete（删除）、file:upload（上传）、file:list（文件查询）、file:reparse（重新解析）、file:delete（文件删除）、kb:search（检索调试）、kb:chat（知识库问答）。普通员工默认仅可授予kb:chat，管理类权限授予管理员角色；后端接口统一绑定权限依赖校验，无权限返回403并记录审计日志，前端按钮通过HasPermission权限组件控制显隐。

上传安全：后缀白名单叠加文件头魔数校验，防止伪造后缀；单文件上限50MB；SVG等存在脚本注入风险的格式不纳入白名单；同库哈希去重防止重复入库；原始文件不对外暴露真实路径，下载走鉴权接口。操作审计：上传、删除、重建索引、检索调试等关键操作统一写入sys_log，仅可查询不可修改。

### 11\.11 分期实施与测试要点

一期完成核心链路：TXT、Markdown、PDF、Word解析入库，知识库与文件管理界面，软删除机制，多轮查询改写，基础向量检索，带引用的流式问答。二期完成质量增强：BM25混合检索与RRF融合、重排与阈值过滤、Excel与CSV及图片多模态入库、检索调试页面完善。三期完成Agent整合：retrieve工具注册进LangGraph Agent与AI助手统一编排、CRAG自纠错检索、重建索引运维工具、检索质量评估集。

测试要点：入库链路校验各格式文件解析状态流转、重复上传去重、失败原因记录与重新解析；管理功能校验增删改查、切片预览、软删除后检索即刻不可见、恢复后重新命中；检索问答校验多轮改写、引用溯源字段正确、无关问题明确回答不知道、SSE流式输出与引用列表完整返回；权限校验无权限调用返回403并记录审计日志。

## 十二、部署与运行说明

### 12\.1 本地开发运行方式

本地开发环境采用专属启动方式，后端统一基于FastAPI框架，通过uvicorn启动ASGI服务，组织架构核心业务与AI能力（LangGraph Agent、NL2SQL、RAG）同服务承载，无需双框架协同运行。前端通过指定端口启动Vite开发服务，启动后前端页面可通过本地IP与端口访问，同时支持局域网设备访问调试。

### 12\.2 数据库配置说明

系统数据库统一使用MySQL，连接参数通过\.env环境文件统一配置。数据库字符集统一配置为utf8mb4，支持全类型字符存储；数据表结构变更统一通过Alembic迁移脚本管理，保证多环境表结构一致。数据库账号、密码、地址、端口、库名等敏感配置统一存放于环境文件，不硬编码在代码中，保障数据安全，文档中不记录真实数据库密码。

## 十三、测试检查清单

每次修改组织架构模块代码或功能后，需完成全维度功能校验，保障模块稳定运行。基础登录功能需校验正确账号正常登录、错误账号触发限流与日志记录；用户信息接口需正常返回用户资料、菜单树与权限列表。

权限体系需校验动态菜单差异化展示、无权限按钮隐藏、越权接口访问返回403并记录日志。用户、部门、角色、菜单基础增删改查、状态启停、排序功能需正常生效。注册流程需校验普通员工自动通过、特殊身份需审批的规则。

入职邀请全流程、密码找回限流、图片上传展示、操作日志记录等功能需完整校验，确保所有核心业务流程正常运行。可通过系统内置检测命令校验代码规范与语法错误，保障代码质量。

## 十四、与初期设计的主要差异

模块初期仅实现基础的用户、部门、角色、菜单、权限管理功能，当前版本已全面迭代升级，拓展了注册审批、入职邀请、个人中心、全量操作审计、系统配置等核心能力，从单纯的组织管理模块升级为系统基础权限平台。

图片资源存储方式全面优化，摒弃本地路径存储模式，改为数据库Data URL存储，彻底解决跨设备部署资源丢失问题。运行环境不再强制依赖Docker与Redis，本地开发可轻量化部署。

注册机制全面优化，区分普通员工与特殊角色，实现差异化注册审批流程。权限体系升级为后端RBAC鉴权、前端动态路由、按钮权限指令的三重管控模式，安全性大幅提升。菜单路由不再固定配置，完全由数据库菜单树动态驱动，适配灵活的权限配置场景。日志记录从简单查询记录，升级为全流程、多维度的操作审计体系，覆盖安全拦截、审批、邀请、接口异常等所有关键场景。

## 十五、变更记录

v1\.0版本为初期设计，仅完成组织架构模块基础功能设想，搭建基础用户、部门、权限框架。

v2\.0版本更新于2026年06月01日，补充完善用户、角色、菜单、权限、部门、日志、系统配置等核心功能设计，完善模块基础架构。

v3\.0版本更新于2026年06月18日，完全对齐当前系统源码，新增注册审批、入职邀请、图片数据库存储、动态路由、登录与密码找回限流、全维度操作审计等能力，完成模块全功能迭代升级。

v4\.0版本更新于2026年09月04日，完成整体技术栈迁移设计：前端由Vue 3迁移至React（Ant Design \+ Zustand \+ React Router），后端由Django迁移至FastAPI（SQLAlchemy \+ Alembic \+ PyJWT），数据库统一为MySQL；AI Agent确定采用LangGraph框架编排，RAG向量数据库确定选用Chroma，RAG模块详细设计待后续单独补充。

v4\.1版本更新于2026年09月04日，新增"十一、RAG知识库模块设计"章节：明确FastAPI \+ Chroma \+ LangGraph \+ MiMo-V2.5的模块架构与入库、查询双链路，确定解析、切分、Embedding、检索三期演进方案，输出知识库、文件、切片三张表结构字段明细与全量接口清单、权限码，确立软删除与引用溯源规则；原部署、测试、差异、结论章节顺延重编号。

## 十六、结论

当前组织架构模块已成为企业智能分析平台的核心基础平台层，承担全系统身份认证、组织管理、权限管控、账号准入、安全防护、操作审计的核心能力。模块通过成熟的RBAC权限体系、动态菜单路由、精细化按钮权限控制、规范化注册审批与入职流程、全方位安全限流与审计机制，为考勤、薪资、AI智能中心、RAG知识库等所有上层业务模块提供稳定、安全、统一的基础支撑。

后续所有新增业务模块接入系统时，需统一遵循现有规范：在菜单管理中配置对应功能目录、页面与按钮权限，通过角色管理完成权限授权；后端接口必须绑定对应权限校验规则，前端功能按钮统一使用权限指令管控；所有业务数据关联系统用户与部门体系，关键操作留存审计日志，保障全系统架构统一、权限规范、可审计、可追溯。

> （注：部分内容可能由 AI 生成）
