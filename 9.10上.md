# 2026-09-10（上午）改动记录

> 本轮对话围绕 **AI 助手 / 知识库 / 全局布局** 三个方向，共处理 10 个主题：5 个功能与体验优化、4 个缺陷修复、1 个成本优化。
> 基线：后端 `pytest tests/` **128 passed**；前端 `npx tsc -b` 通过、`npx vitest run` **18 passed**、`oxlint` 0 错误。

---

## 一、AI 助手页面体验优化（第一批）

| 文件 | 改动 |
| --- | --- |
| `frontend/src/views/ai/chat/index.tsx` | 新增**滚动跟随**（仅在底部时自动吸底）+ **「回到底部」悬浮按钮**（未读红点）；打开会话新增**骨架屏**；硬编码 `#f0f0f0` → `var(--ant-color-split)` |
| `frontend/src/views/ai/chat/useChat.ts` | 流式增量按 **`requestAnimationFrame` 节流**合并（长回答降低重渲染次数）；新增 `loadingConversation` 打开会话加载态；`onDone` 由全量重拉列表改为 **`touchConversation` 本地更新** |
| `frontend/src/views/ai/chat/ChatInput.tsx` | 输入区/图片边框硬编码颜色 → antd cssVar（暗色主题适配） |

---

## 二、GLM-4-Flash「本次未生成有效回答」修复

### 2.1 首次尝试（未命中真因，保留部分有用能力）
| 文件 | 改动 |
| --- | --- |
| `backend/app/services/ai_model_service.py` | `_config_of()` 与 `.env` 回退分支补充 `provider` 字段 |
| `backend/app/services/llm_client.py` | 新增 `_thinking_field()`（智谱 `thinking.type` 控制）与 `_extract_stream_error()`（识别 HTTP 200 流内嵌 `error` 块）；`achat_stream` / `achat_with_tools` 新增 `thinking` 参数 + 「不支持该参数则去掉重试」的请求体降级 |

### 2.2 真因定位与修复（核心）
**真因**：`_generate_node` 组请求时 `[system] + state["messages"]`，而 `state["messages"]` 末尾是 agent 已产出的 assistant 终答 → 请求以 `assistant` 结尾，GLM-4-Flash 视为轮次结束返回**空正文**（实测 `len 0`；剔除末尾 assistant 后立刻正常返回）。
MiMo 容忍该形态，故此前未暴露。

| 文件 | 改动 |
| --- | --- |
| `backend/app/services/ai_chat_service.py` | ① `_generate_node` 构造请求前**剔除末尾纯文本 assistant 消息**（保留 `assistant.tool_calls` + `tool` 配对）；② `_route_after_agent` 改为：agent 已作答且**无 `context_chunks`**（nl2sql/server_admin）→ 直接 `direct`，有检索正文才走 `generate`；③ `_direct_node` 在深度思考时补发 agent 的 `reasoning_content`；④ 输出预算回退为 3072/4096（深度思考 8192/16384）；⑤ 空回答文案改中性 |
| `backend/tests/test_ai_chat_routing.py` | **新增 6 用例**：路由决策（tools/direct/generate）+ `_generate_node` 请求不以 assistant 结尾 |

---

## 三、NL2SQL 幻觉 `status` 过滤导致 0 行（A+B）

**真因**：glm-4-flash 自行给 `sal_payroll` 加了 `WHERE status = 1`，但该列是**业务状态（0草稿/1已确认/2已发放）**，演示库无 `status=1` → 0 行 → 模型如实回答"没有查询到"。

| 文件 | 改动 |
| --- | --- |
| `backend/app/services/nl2sql_service.py` | `_TABLE_DDL` 标注 `sal_payroll.status` / `att_record.status` 为**业务状态、非软删除**；`_SYSTEM_PROMPT` 第 4 条硬性约束「**除用户显式要求状态筛选，否则禁止添加任何 status 条件**」；新增「薪资最高/最低」「已发放的工资单」正反示例 |
| `backend/app/services/ai_chat_service.py` | `_tool_nl2sql` 在 **0 行时追加自纠提示**（提示去掉多余筛选后重查一次），借助 `agent ⇄ tools` 循环自我纠正 |
| `backend/tests/test_nl2sql_prompt.py` | **新增 4 用例**：提示词约束、0 行自纠提示、有数据不提示 |

**实测**：连问 3 次「薪资最低的人是谁？」→ 3/3 均无多余 status 过滤；端到端「薪资最低→刘洋（1 行）」「薪资最高→个人乙（1 行）」「已发放的工资单→2 行（正确使用 status=2）」。
**非过度纠正**：用户显式要求状态时仍会正确过滤。

---

## 四、AI 对话界面滚动条优化

**根因**：`MainLayout` 的 `Content` 同时容纳面包屑与 `height:100%` 的聊天页，溢出约 38px → 出现最右侧外层滚动条，且聊天面板底部被裁。

| 文件 | 改动 |
| --- | --- |
| `frontend/src/layouts/MainLayout.tsx` | `Content` 改**纵向 flex、自身不滚动**；面包屑固定顶部，页面内容放入 `flex:1; minHeight:0; overflowY:auto` 内层滚动容器 |
| `frontend/src/index.css` | 新增 `.chat-scroll`：**细窄 + 悬停才显示**的滚动条，颜色走 antd cssVar（明暗主题自适应） |
| `views/ai/chat/index.tsx`、`views/ai/chat/ConversationSidebar.tsx`、`views/ai/kb/chat/index.tsx` | 4 个滚动容器加 `chat-scroll` 类 |

> 行为变化：面包屑改为固定在内容区顶部（不再随页面滚走）。

---

## 五、侧栏折叠后内容区不跟随（既有 bug）

**根因**：`MainLayout.tsx` 中 `marginLeft: SIDER_WIDTH` 写死 200，不随 `collapsed` 变化；Sider 为 `position:fixed`，折叠到 80 后左侧空出约 120px 空白带。

| 文件 | 改动 |
| --- | --- |
| `frontend/src/layouts/MainLayout.tsx` | 新增 `SIDER_COLLAPSED_WIDTH = 80`；`marginLeft` 改为 `collapsed ? 80 : 200` + `transition: margin-left 0.2s`；修正误导性注释 |

> `git diff` 确认该行属既有代码，非上一轮滚动条改动引入。

---

## 六、「回到底部」按钮误显示（逻辑 bug）

**根因**：打开会话时**无条件** `setShowJump(true)`（假设历史会话一定很长）；短会话内容不足一屏、容器不可滚动，本就"在底部"，且不可滚动时**不触发 `scroll` 事件**，没有任何机会纠正。

| 文件 | 改动 |
| --- | --- |
| `frontend/src/views/ai/chat/index.tsx` | 抽出 `syncScrollState()`：**不可滚动一律视为在底部**并隐藏按钮；打开会话按"是否可滚动"分支；新增 **`ResizeObserver`** 监听消息区尺寸变化自动纠正 |

---

## 七、知识库管理优化 · 阶段 1（A 性能 + B 管理能力）

| 文件 | 改动 |
| --- | --- |
| `backend/app/services/kb_service.py` | **A1**：新增 `_kb_counts()`（两条 `GROUP BY` 批量统计），`serialize_kb(db, kb, counts=None)` 支持复用，`list_kbs` 改批量 → 整页 SQL **从 `2+2N` 降为固定 4 次**（20 行时 42 → 4）；**B2**：`list_chunks(keyword=)` 按切片正文模糊过滤；**B5**：`update_kb` 检测 `chunk_size/chunk_overlap` 变化 → 返回 `requires_rebuild` |
| `backend/app/routers/kb.py` | `GET /kb/files/{id}/chunks` 增加 `keyword` 参数 |
| `frontend/src/api/kb.ts` | `update` 返回 `KBBase & { requires_rebuild?: boolean }`；`chunks` 增加 `keyword` |
| `frontend/src/views/ai/kb/index.tsx` | 切片预览抽屉新增**搜索框**（搜索回第 1 页）；保存时若 `requires_rebuild` 弹 warning；编辑弹窗补充提示文案 |
| `backend/tests/test_kb_service.py` | **新增 4 用例**：批量统计、软删过滤、切片关键词、需重建标记 |

**顺带发现（未改）**：软删「文件」的切片仍被计入 `chunk_count`（原实现未 `JOIN kb_file`），`rebuild_kb_vectors` 也会重嵌入软删文件的切片。

---

## 八、上下文统计真实性（D + A + C）

**结论**：token 用量/耗时/上下文「已用总量」是**真实**的；圆环**分项占比是估算**；窗口是**配置值**；缓存命中率在 glm 上**无数据**。

| 文件 | 改动 |
| --- | --- |
| `backend/app/services/ai_model_service.py` | **A**：新增 `infer_context_window()` + 已知模型窗口表（glm-4.5/4.6/4.7/4/5/z1 → 128000，mimo → 1000000）；`_config_of` 与 `list_enabled_llm_models` 使用。实测 `glm-4.7-flash` 由 `None → 128000`（不再回落 32K 导致占比虚高） |
| `backend/app/services/ai_chat_service.py` | **C**：把 `prompt_max`、`cached_total`、`cache_hit_rate` 提前计算并写入 `usage_stats`（随消息落库的 `context_tokens` / `cache_hit_rate`），`done` 事件复用同一值；**D**：`_estimate_conversation_context` 优先取最近一轮真实 `context_tokens`（单次最大 prompt，非全轮累加计费口径）并按比例缩放分项，缺失才退回估算，同时返回 `cache_hit_rate` |
| `frontend/src/api/aiChat.ts` | `context_usage` 增加 `cache_hit_rate?: number \| null` |
| `frontend/src/views/ai/chat/useChat.ts` | 打开会话时用 `backend.cache_hit_rate` 填充圆环浮层 |
| `backend/tests/test_ai_context_usage.py` | **新增 6 用例**：窗口推断/配置优先/未知返回 None/真实占用优先/无 usage 退回估算/旧累加口径不高估 |

**验证证据（会话 #91 落库）**：`usage = {"prompt_tokens":847,"context_tokens":847,"completion_tokens":54,"estimated":false,"cache_hit_rate":null}`，与 UI「847/12.8万」「输入 847 / 输出 54」完全一致。

---

## 九、工具结果 0 显示修复 + 提示词精简（省 token）

| 文件 | 改动 |
| --- | --- |
| `backend/app/services/ai_chat_service.py` | ① 两处缩放 `max(1, round(...))` → **保留真实 0 值**（原会把 0 兜底成 1，浮层出现「工具结果 1」假象）；② **`AGENT_SYSTEM_PROMPT` 精简**：删除与 `TOOLS_SPEC` 重复的工具清单+四表列举，保留全部规则（371 → 122 字符）；③ `server_admin` 描述去掉与 `action.enum` 重复的动作列表、`nl2sql` 描述去掉冗余举例 |
| `frontend/src/views/ai/chat/ChatInput.tsx` | `ContextPopover` 过滤 `tokens === 0` 的分项（非零才展示） |
| `backend/tests/test_ai_context_usage.py` | 增加「0 值保持」用例 |

**精简效果**：固定开销估算 **714 → 519 tokens（−195，约 −27%）**。
**行为回归验证（真实 glm-4-flash）**：

| 问题 | 期望 | 实测 |
| --- | --- | --- |
| 公司的年假制度是什么？ | retrieve | ✅ `retrieve` |
| 薪资最低的人是谁？ | nl2sql | ✅ `nl2sql` |
| 你好，你是谁？ | 无工具直接答 | ✅ 无工具 |
| 查一下服务器磁盘占用 | server_admin | ✅ `server_admin` |

> 未改动任何约束：`每次只调一个工具 / 结果足够不再调 / 无关问题不调工具`、nl2sql 的 status 约束、`GENERATE_SYSTEM_PROMPT`、`_SYSTEM_PROMPT`。

---

## 十、改动文件汇总

### 后端
| 文件 | 类型 |
| --- | --- |
| `app/services/ai_chat_service.py` | 改（多主题） |
| `app/services/ai_model_service.py` | 改 |
| `app/services/llm_client.py` | 改 |
| `app/services/nl2sql_service.py` | 改 |
| `app/services/kb_service.py` | 改 |
| `app/routers/kb.py` | 改 |
| `tests/test_ai_chat_routing.py` | **新增** |
| `tests/test_nl2sql_prompt.py` | **新增** |
| `tests/test_kb_service.py` | **新增** |
| `tests/test_ai_context_usage.py` | **新增** |

### 前端
| 文件 | 类型 |
| --- | --- |
| `src/layouts/MainLayout.tsx` | 改 |
| `src/index.css` | 改 |
| `src/api/aiChat.ts` | 改 |
| `src/api/kb.ts` | 改 |
| `src/views/ai/chat/index.tsx` | 改 |
| `src/views/ai/chat/useChat.ts` | 改 |
| `src/views/ai/chat/ChatInput.tsx` | 改 |
| `src/views/ai/chat/ConversationSidebar.tsx` | 改 |
| `src/views/ai/kb/index.tsx` | 改 |
| `src/views/ai/kb/chat/index.tsx` | 改 |

---

## 十一、测试基线变化

| 阶段 | 后端 pytest | 前端 vitest |
| --- | --- | --- |
| 本轮开始 | 107 | 18 |
| 空回答链路修复后 | 113 | 18 |
| NL2SQL 提示词后 | 117 | 18 |
| 知识库阶段 1 后 | 121 | 18 |
| 上下文真实性后 | 127 | 18 |
| 工具结果/提示词精简后 | **128** | **18** |

---

## 十二、遗留事项

1. **知识库管理优化 阶段 2~4 未做**：B1 文件下载/预览、B4 知识库启用/停用（含检索口径统一为 `status == 1`）、B3 批量操作；A3 上传流式写盘；A2 重建索引异步化 + 进度、A4 解析自动重试（后两者需 Alembic 迁移）。
2. **软删文件的切片仍计入 `chunk_count`**（且会被 `rebuild_kb_vectors` 重嵌入），建议后续统一为「有效切片 = 切片未删 且 所属文件未删」。
3. **圆环分项仍为估算占比**（方案 B：引入真实 tokenizer 或标注"估算"）未做。
4. 遗留诊断脚本未删除：`backend/_tmp_diag_glm.py`、`_tmp_diag_glm2.py`、`_tmp_diag_glm3.py`、`_tmp_users.py`。
5. `ai助手优化.md` 中的计划已基本落地，建议同步更新以免误导。
