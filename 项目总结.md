# 企业管理系统项目总结

> 依据项目全部 39 次里程碑提交（M0-T1 → M6-T7）、后端 70+ 个 Python 模块、前端 55 个源文件逐章撰写。
> 每章结构：**做了什么 → 用了什么技术、为什么 → 遇到的难点 → 怎么解决的**。

---

## 第一章 项目骨架与技术选型（M0）

### 做了什么
初始化仓库，搭建前后端骨架：后端 FastAPI 入口挂载 CORS、统一响应、全局异常处理与健康检查；Alembic 完成首个空迁移。前端用 Vite 创建 React-ts 项目，封装 axios、用户状态与主布局（侧边菜单 + 顶栏 + 内容区）。

### 技术选型及理由

| 选型 | 为什么 |
| --- | --- |
| FastAPI | 自带 /docs 接口文档、Pydantic 入参校验、依赖注入体系，比 Django 更轻且贴合"前后端分离 + JSON API"形态 |
| SQLAlchemy 2.0（sync）+ pymysql | 同步模式心智负担低、配合 FastAPI 的线程池足够用；实训规模无高并发，不引入 async 复杂度 |
| Alembic | 表结构演进全部走迁移脚本（最终 13 个版本、29 张表），换机可 `upgrade head` 一键重建，杜绝手工 SQL 脚本 |
| React 18 + TS + antd 5 + Vite | antd 5 的 ConfigProvider 主题算法为后续暗色模式埋下伏笔；Vite 开发热更新快 |
| zustand | 比 Redux 轻量得多，token/用户/菜单/权限一个 store 解决，无模板代码 |
| MySQL 8（utf8mb4） | 课程要求的关系型主库；utf8mb4 避免中文与 emoji 乱码 |
| 不引入 Redis/Docker/MQ | 开发文档硬性约定——实训规模下进程内方案即可满足，控制运维与理解成本 |

### 难点与解决
- **前后端响应格式不统一会导致前端到处写兼容逻辑** → 定下全局硬规则：接口统一 `{code, message, data}`、分页统一 `{list, total, page, page_size}`，并在 `app/main.py` 注册三个全局异常处理器（HTTPException / RequestValidationError / 兜底 Exception），任何抛错都以统一结构返回，前端拦截器只写一处。
- **跨域联调** → CORS 白名单只放行 `localhost:5173 / 127.0.0.1:5173`，不开 `*`，配合 `allow_credentials`。
- **删除功能的语义** → 全局约定一律软删除（status=2），前端删除后仅不再显示，所有查询恒过滤 `status != 2`，该约定贯穿后续全部模块。

---

## 第二章 认证与 RBAC 权限体系（M1）

### 做了什么
16 张 `sys_*` 表迁移与预置数据播种；登录（限流锁定）、双 Token 无感刷新与轮换、退出黑名单、当前用户聚合接口；前端登录页 + 动态路由；用户/部门/角色/菜单四组管理页，按钮级权限。

### 技术及理由
- **JWT 双 Token（access 30 分钟 / refresh 7 天）**：access 短命降低泄露风险，refresh 长命免频繁重登；payload 带 `type` 与 `jti`（UUID），为轮换与作废打基础（`app/core/security.py`）。
- **Passlib bcrypt**：密码哈希自带盐与慢速因子，是行业默认选择。
- **依赖注入做权限校验**（`app/core/deps.py` 的 `require_permissions("user:list")` 工厂）：路由函数一行声明所需权限码，校验逻辑、审计埋点全部收敛在一处——这是 FastAPI DI 体系的教科书式用法，避免每个接口手写 if。
- **限流计数存数据库（账号维度）+ 进程内存（IP 维度）**：账号锁定状态 `failed_login_count/locked_until` 落 `sys_user` 表可持久化、可审计；IP 计数用进程内 dict，是因为约束不允许引入 Redis，重启清空可接受。
- **迁移内播种**：`a1f2e3d4c5b6` 迁移以 `bulk_insert` 写入 admin 账号（bcrypt 哈希硬编码于迁移）、3 角色、87 条菜单、82 个权限码——保证"全新库迁移完即可登录"，无需额外 seed 脚本。

### 难点与解决
1. **表外键成环**：`sys_department.leader_id` 指向 `sys_user`，而 `sys_user.department_id` 又指向 `sys_department`，建表顺序死锁。解决：先以普通列创建部门表，待用户表建好后 `ALTER TABLE` 补外键（迁移文件头注释明确记录）。
2. **refresh 令牌泄露后如何作废**：JWT 本身无状态、签出即有效。解决：引入**轮换 + 进程内 jti 黑名单**——每次刷新校验旧 refresh 后立即把其 `jti` 拉黑并签发新对；退出同样拉黑。旧令牌即使未过期也立刻失效。
3. **并发请求触发的 401 风暴**：access 过期时页面往往同时发多个请求，各自刷新会导致后端轮换后旧 refresh 失效、后续刷新全部 401。前端解决（`src/api/request.ts`）：`refreshPromise` 单例让并发 401 只触发一次刷新、其余请求等待同一 Promise；`_retried` 标记保证原请求只重放一次；刷新用**无拦截器的裸 axios 实例**，避免刷新请求自身再进 401 逻辑形成循环。
4. **账号枚举**：登录时对"账号不存在"与"密码错误"返回同一句"账号或密码错误"，防撞库探测。
5. **动态路由怎么和后端菜单打通**：后端下发菜单树（目录/页面/按钮三类，按钮不下发前端），前端 `viewMap` 把菜单的 `component` 字符串（如 `views/org/user/index`）映射到实际组件并递归生成 `<Route>`；未实现的页面统一落占位页。路由守卫只在 `token` 存在时渲染主布局，避免闪跳。
6. **越权调用要留痕**：`require_permissions` 校验失败时先 `write_log` 记录"权限拦截"（操作人、路径、缺少的权限码）再抛 403，管理端可在操作日志中审计。

---

## 第三章 业务模块：职位 / 考勤 / 薪资 / 个人中心（M2）

### 做了什么
职位 CRUD 并绑定角色作为"权限模板"（新用户选职位自动继承角色）；考勤 Excel 导入、覆盖更新、手动补录、规则维护；按月生成工资单（三项构成）、确认/发放；个人中心（资料/头像/改密/我的考勤/我的薪资）。

### 技术及理由
- **金额全程 DECIMAL(10,2)**：薪资是钱，浮点误差不可接受。服务层用 `Decimal("0")` 做累加起点、`Decimal(str(data.amount))` 入库、序列化一律 `str()`，杜绝 float 混入。
- **唯一索引兜底业务约束**：考勤 `(user_id, att_date)`、工资单 `(user_id, year_month)` 都建唯一索引，代码层 upsert 之外还有数据库层保险。
- **pandas/openpyxl 读 Excel**（`app/utils/excel.py`）：模板列固定为「员工账号/日期/签到/签退/状态/地点」，逐行校验。

### 难点与解决
1. **真实世界的 Excel 脏数据**：日期有 `2026-9-1`、`2026/9/1`、甚至带时间后缀等多种形态。解决（`attendance_service._parse_date/_parse_time`）：多格式循环尝试（`%Y-%m-%d / %Y/%m/%d / %Y.%m.%d`；时间 `%H:%M:%S / %H:%M`），先按空格切掉日期/时间前缀再解析；考勤状态同时接受中文「迟到」与英文键 `late`。
2. **导入不能"一条错、整表死"**：逐行校验，错误行跳过并收集 `{row, reason}` 报告返回前端；同一文件内同员工同日重复行后者覆盖前者，与库内已有记录也做覆盖更新（`_upsert_record`），导入结果返回 `total/success/failed/overwritten/errors` 五项统计。
3. **工资单"重算"与"锁定"的边界**：生成接口只覆盖 `status=0` 草稿；已确认/已发放的工资单跳过并计入 `locked` 返回给前端提示。职位基本工资与职位名在生成时**快照**进 `sal_payroll`，之后职位调薪不影响历史工资单；而明细页的逐条考勤/奖惩明细按当前数据实时计算展示——快照保正确性、明细保可追溯。
4. **普通员工的数据隔离**：我的考勤/我的薪资接口直接以登录态 `user.id` 查询，不接受前端传 user_id，从接口层面杜绝越权看他人薪资。

---

## 第四章 RAG 知识库（M3 一期 + M6 检索增强二期/三期）

### 做了什么
知识库/文件管理（上传校验、软删、重建索引）；pdf/docx/md/txt 解析、500/80 切分、智谱 embedding-3 向量化、Chroma 入库；检索问答（多轮改写、[n] 引用、SSE 流式、软删不可见）；二期混合检索（BM25+RRF+rerank）与三期 CRAG。

### 技术及理由
- **Chroma（PersistentClient，一库一 collection `kb_{id}`，cosine 空间）**：嵌入式向量库，无需独立服务，持久化到 `data/chroma`，符合"不引重型中间件"约束。
- **双写设计**：切片文本与溯源元数据写 MySQL `kb_chunk`，向量写 Chroma——MySQL 是事实源，Chroma 可随时从 MySQL 重建（`rebuild_kb_vectors`），解决了"向量库坏了怎么办"的可用性问题。
- **pdfplumber / python-docx**：pdfplumber 能同时抽文本和表格且带页码；docx 按标题样式维护**标题路径栈**（`一级>二级>三级`），切片自带章节溯源。
- **递归字符切分（500/80）**：按分隔符优先级 `\n\n → \n → 。→ ；→ ，→ 空格` 递归细分，超长段硬切兜底，块间携带 80 字符 overlap 保语义连续；表格不走文本切分，按行分组、每组重复表头。
- **BM25 手写 Okapi（k1=1.5, b=0.75）+ jieba 分词**：向量检索对"精确术语/型号/编号"不敏感，关键词检索补这一路；不引 Elasticsearch，5000 片规模内纯 Python 计算毫秒级。
- **RRF 融合（k=60）**：两路检索的分数量纲不可比（余弦相似度 vs BM25 分），RRF 只用排名，`1/(60+rank)` 相加即可稳健融合，是业界默认常数。
- **CRAG 简化版**：先用 LLM 判"资料充分/不足"（`max_tokens=8, temperature=0`，便宜且快），不足则让 LLM 改写检索词重检，最多两轮——用最小成本拿到自校正检索的大部分收益。

### 难点与解决
1. **上传安全**：只验后缀可被伪造。解决：后缀白名单 + **文件头魔数校验**（pdf 必须 `%PDF-`、docx 必须 zip 容器 `PK\x03\x04`）+ 50MB 上限 + 同库 SHA256 哈希去重（`kb_service.py`）。
2. **异步解析的状态可见性**：解析+向量化耗时数十秒，不能同步阻塞请求。解决：BackgroundTasks 跑 `process_file`（独立 `SessionLocal`，避免请求会话提前关闭），文件状态机 `0待解析→1解析中→2已入库/3失败`，失败原因 `fail_reason` 落库（截 250 字符），前端轮询状态、失败可重试（`parse_status in (0,3)` 才可再跑）。
3. **软删文件在向量库中"阴魂不散"**：Chroma 的 metadata 过滤不可靠且 BM25 语料另有来源。解决：检索后在 **MySQL 侧二次过滤**——把候选 `file_id` 批量查库校验 `status != 2`，两路候选（含 BM25 独有）都过这道闸，软删即不可见。
4. **黑名单词误杀正常内容**：NL2SQL 与检索都遇到过"字面量里有危险词"的问题。检索侧的典型坑是**相似度字段语义混杂**——重排后是 rerank 相关性分、未重排是余弦相似度、BM25 独有候选干脆没有分数。解决：`similarity` 字段设计为可空并在文档注释中明确语义，前端按可空兼容展示。
5. **BM25 语料的重复构建开销**：每次检索全量分词太浪费。解决：`_bm25_cache` 以 `frozenset(kb_ids)` 为键缓存 token 化语料，用"(切片总数, 最大切片 id)"判断失效——新上传/删除会改变这两个值，缓存自动失效重建，无需手动清。
6. **rerank 挂了不能阻塞问答**：调 `/rerank` 失败或未配置 rerank 模型时返回 `None`，调用方自动降级为 RRF 融合序继续检索——"宁少勿滥"（阈值 0.2 以下丢弃）是精排质量与召回的折中。
7. **多轮对话里"它说的那个是什么"**：后续问题常依赖上文（"那第二条呢"）。解决：取最近 3 轮历史 + 改写 prompt 生成独立检索词；改写结果超过 120 字或以"抱歉/作为"开头视为 LLM 失控，回退原问题——对生成结果做防御性校验。

---

## 第五章 AI 智能中心（M4：模型配置 / NL2SQL / AI 助手）

### 做了什么
模型配置（Fernet 加密、连通性测试、默认切换）；NL2SQL（自然语言生成 SQL → 人工审核 → 只读执行 → 历史）；AI 助手（LangGraph Agent：知识库检索 + NL2SQL + 服务器管理工具，SSE 流式、深度思考、会话持久化）。

### 技术及理由
- **Fernet 对称加密存 api_key**（`app/utils/crypto.py`）：密钥在 `.env` 的 `FERNET_KEY`，库里只存密文；序列化只回显掩码 `****后4位`，更新时留空即保持原 key。加密库选 cryptography 的 Fernet 是因为带认证（防篡改）且 API 极简。
- **LangGraph 而非裸循环**：`StateGraph` 显式编排 `agent ⇄ tools → generate`，State 用 `TypedDict + operator.add` reducer，多轮工具调用增量合并免手工拼接；`recursion_limit=50` + `MAX_TOOL_ROUNDS=6` 双层兜底防图失控。
- **OpenAI 原生 function calling，不用 LangChain bind_tools**：自维护 `TOOLS_SPEC` JSON Schema（retrieve / nl2sql / server_admin 三个工具），节点内手工解析执行。原因：推理型模型回放带 tool_calls 的 assistant 消息时**必须原样携带 reasoning_content 否则上游接口报错**——自己管消息字典才能保证回放完整性，绕开抽象层的兼容坑。
- **httpx 手写 OpenAI 兼容调用**（`llm_client.py`）而非 openai SDK：三个函数（chat_stream / chat_once / chat_with_tools）直接对接 `/chat/completions`，逐行解析 `data:` SSE 与 `[DONE]`，把 `reasoning_content` 与 `content` 分离 yield——对"推理模型只吐思考不吐正文"这类边角行为可控可试。
- **NL2SQL 纵深防御四层**：① prompt 六条硬约束（仅一条 SELECT、仅 product 表、恒带 `status!=2`、LIMIT≤100、不带库前缀不加分号——最后一条是特意为正则校验扫清障碍）；② 生成与执行共用同一纯函数 `validate_and_normalize_sql`（黑名单正则覆盖写操作/DDL/文件注入/时间盲注/系统库 + 表白名单 + LIMIT 归一化）；③ 会话级 `SET SESSION TRANSACTION READ ONLY`；④ 独立连接池 + `connect_timeout/read_timeout=5s` + `MAX_EXECUTION_TIME=5000`。

### 难点与解决
1. **"WHERE description LIKE '%delete%'" 被黑名单误杀**：危险词扫描会命中字符串字面量。解决：手写 `_strip_literals` 字符串扫描器（处理反斜杠与双写转义），先把 `'...'` 替换为空串再做黑名单扫描——校验规则与误杀问题一起被 15 个单测覆盖（`tests/test_nl2sql_validation.py`）。
2. **审核后 SQL 被篡改 / 历史数据不合规（TOCTOU）**：执行前用同一校验函数**再校验一次**；仅 `status==0` 可审、仅 `status==1` 可执行、`status==3` 禁重复执行，状态机卡死每一步。
3. **推理模型只吐思考不吐 SQL**：`chat_once` 低 token 下返回空。解决：自动把 max_tokens 加倍（2048→4096）重试一次，仍空才 422；连通性测试同样特判——content 为空但 reasoning_content 有值时提示"推理模型连通正常"。
4. **LangGraph 里既要流式 delta 又要终态持久化**：用 `stream_mode=["custom", "values"]` 一次流式调用同时拿两类输出——节点内 `get_stream_writer()` 发 custom 事件（tool 通知 / reasoning / message delta / citations / error）转 SSE 帧，values 模式的最后一份全量 state 供落库，省去第二次图执行。
5. **会话时间线错乱**：tool 消息与最终回答若按各节点完成顺序落库可能乱序。解决：图跑完后先统一落全部 tool 消息（截 2000 字符）再落 assistant 终答，保证"工具调用 → 最终回答"的时间线；回放上下文只取最近 4 轮 user/assistant 终答，不含中间 tool 消息，防上下文膨胀。
6. **SSE 流内异常不能 500 了事**：生成器一旦开始，HTTP 状态码已发出。解决：流内所有异常转 `error` 事件下发（"流内异常必须作为事件下发"），并 return 防止连接悬挂。
7. **AI 助手里的 NL2SQL 要不要审核**：助手链路免审核但**留痕**——落 `nl2sql_record`（直接置"已执行"）并写审计日志"AI助手执行SQL"，安全性靠同一套校验 + 只读连接保障，便捷与合规兼得。

---

## 第六章 工作台看板、全量回归与交付（M5）

### 做了什么
工作台数据看板（统计卡片 + echarts 图表，只读聚合接口）；补齐参数配置/字典管理/注册审批/入职邀请四页；全量回归修复；README/.env.example 交付物。

### 技术及理由
- **看板用单聚合接口**（`dashboard:view` 权限）：一次请求返回全部卡片与图表数据，前端 echarts 只做渲染——避免页面挂载时七八个请求各查一遍库。
- **echarts 按需引入封装 `EChart.tsx`**：后续代码分割时 echarts（重型依赖）随路由延迟加载。

### 难点与解决
1. **看板数字对不上账**（回归阶段发现的数据口径 bug）：用户总数原统计包含停用与待审批账号，而部门人数分布只算在职，卡片合计 20 与图表合计对不齐。解决：用户总数改为在职口径（`status=1`），部门人数分布末尾追加「未分配部门」柱（在职但 `department_id` 为空的账号），最终 20 = 13 + 7 严格对账（提交 `b69fbb9`）。教训：**统计口径要在写接口时定义，而不是靠回归发现**。
2. **部门部分更新 422**：全量回归发现 PUT 部门只传部分字段时 Pydantic 必填校验报错，修正 schema 可选性。
3. **员工角色看不到 AI 助手**：菜单授权边界遗漏，用迁移 `d2e3f4a5b6c7` 补授目录+页面菜单——员工"可问答、不可管理"（无知识库管理/NL2SQL/模型配置权限），体现"权限补授也要走迁移"的工程习惯。
4. **AI 回复是 Markdown 原文**：接入 react-markdown + remark-gfm 统一渲染 AI 助手与知识库问答两页，代码块/表格/引用配套样式；生成占位改「正在思考…」三点动画 + 行尾流式光标，体验对齐商业产品。

---

## 第七章 安全与体验增强（M6：找回密码 / 多模态 / 偏好 / 主题 / 一键脚本 / 服务器管理）

### 7.1 找回密码（M6-T1 + 增强）
- **做了什么**：登录页两步重置（验证身份 → 重置密码）；后端验证码生成/校验；SMTP 真实下发（465 SSL / 587 STARTTLS 自适应），失败自动回退演示回显。
- **难点与解决**：
  - 验证码若明文落库，拖库即泄露。解决：库里只存 **bcrypt 哈希**（`code_hash`），10 分钟 TTL；发新码时旧未用码一律作废，保证同一时间仅一个有效码。
  - 验证码接口可被刷。解决：**双维度限流**——单账号 15 分钟 3 次（查库计数，持久化）、单 IP 15 分钟 10 次（进程内计数）；试错 5 次该码作废，错误提示还剩余次数。
  - 演示环境没有邮件服务怎么办：SMTP 未配置直接回显验证码（channel=demo）；SMTP 配置但发送失败时记录失败日志并**回退演示回显**，前端按渠道区分提示、邮箱脱敏展示（`z***@qq.com`）——同一套代码兼容两种运行环境。

### 7.2 AI 助手图片多模态（M6-T2）
- **难点与解决**：
  - 图片存储选型：落文件系统要管生命周期、对象存储超出约束。解决：**Data URL 直存直传**——`ai_message.attachments` JSON 列存 `[{"type":"image","url":dataURL}]`，最多 3 张、单张 4M 字符、正则只放行 png/jpg/webp。
  - 大图直接传会拖垮请求。解决：前端 canvas 压缩，长边 1568（主流视觉模型的最佳输入分辨率）、JPEG quality 0.85（`utils/image.ts`）。
  - 模型不支持视觉怎么办：请求被 422 拒即视为不支持——**剥图重试**，把 content-parts 退化为一句话文本（"用户上传了图片，当前模型不支持…"）。在 agent 与 generate 两个调用点分别处理，且 generate 重试前清空已累积的增量，防止重试后内容重复。
  - 回归修复：重新打开带图历史会话时图片缩略图丢失——消息重建时补齐 attachments 映射（提交 `677fb0e`）。

### 7.3 个人偏好 + 明暗主题（M6-T3/T4）
- **实现**：`sys_user.preferences` JSON 列，PUT 接口按**白名单键**合并（防注入任意键）；默认首页登录后自动跳转，且跳转前校验该路径在用户授权菜单内——否则回退 `/dashboard`，**防止越权死循环**。
- **主题难点**：antd 组件能被 darkAlgorithm 染色，但自己写的气泡/代码块/分隔线是硬编码白色，暗色下刺眼。解决：`ConfigProvider` 开启 `cssVar: true` 暴露 `--ant-*` CSS 变量，所有自定义样式的硬编码颜色替换为变量（如 `--ant-color-bg-container`），跟随算法自动明暗联动；后续还修复了聊天页两处漏改的面板底色（`45b4d05`）。

### 7.4 一键启动/停止脚本（M6-T4）
- **难点与解决**：重复双击导致端口冲突、迁移不同步、cmd 中文乱码。解决（`start-all.bat`）：`netstat` 检测 3306/8000/5173 端口——已占用则跳过并提示，实现**幂等启动**；启动后端前先 `alembic upgrade head` 同步迁移；`ping -n 7` 充当延时后自动开浏览器；stop-all 按端口强杀。文件以 GBK 编码 + CRLF 换行保存，兼容 cmd 解析。

### 7.5 AI 助手服务器管理（M6-T6，MCP 风格只读探查）
- **做了什么**：LangGraph 新增 `server_admin` 工具，6 个 action 白名单：系统信息 / 磁盘 / 进程 / 网络统计 / 项目目录浏览 / 文本文件读取；前端橙色工具标签展示动作与路径。
- **难点与解决**（这是全项目安全设计最密集的一块）：
  - 让 LLM 触碰文件系统风险极高。解决：**文件沙箱**——`_sandbox_resolve` 把相对路径 resolve 后校验"目标必须位于项目根内"（`base in target.parents`），`../` 穿越直接拒绝；文本读取 100KB 上限、返回给模型再截 4000 字符、前 200 字符含 `\x00` 判二进制拒绝、utf-8 失败回退 GBK。
  - 全部 action 只读：进程用 `tasklist`（Windows）/`ps aux`（Unix），网络用 `netstat -ano` 解析统计，subprocess 统一 15 秒超时。
  - 权限门控**双保险**：路由层查询 `ai:server_admin` 权限码随 state 注入图；tools 节点执行前二次检查，无权限返回固定引导话术（模型可转述但拿不到数据）。权限码由迁移播种为 AI 助手下按钮型子菜单，仅授予 admin 角色（超管经 `role_type=1` 自动全量）。

### 7.6 性能与工程化
- **首屏 784KB → 141KB（gzip）**：25 个页面全部 `React.lazy` + Suspense 按页分包，echarts 等重型依赖随路由延迟加载（提交 `a6d8460`）。
- **lint 清零**：`usePermission` hook 内联进 `HasPermission` 组件——react-refresh 要求组件文件只导出组件；对 `set-state-in-effect` 规则做项目级豁免，因为"20+ 页面挂载即请求"是既有约定模式，逐页重构风险大于收益——**工程决策有明确权衡而非无脑服从规则**。

---

## 第八章 自动化测试（M6-T7）

### 做了什么
pytest 测试基建 + 四条核心链路 36 个用例全绿：NL2SQL 安全校验（注入/多语句/限表限行）、登录认证（限流/刷新轮换/黑名单）、工资单计算（三项构成/确认锁定）、找回密码（发码限流/试错/过期）。

### 技术及理由
- **独立测试库 `enterprise_test`**：绝不碰开发库；不存在时自动 `CREATE DATABASE` 并 `alembic upgrade head`（连 admin 账号、菜单、考勤规则种子数据一并就位）。
- **savepoint 回滚实现用例零污染**：`db_session` fixture 用 `join_transaction_mode="create_savepoint"` 在单个连接事务内建会话——**服务层代码里的 `db.commit()` 被收敛为 savepoint 释放**而非真提交，用例结束统一 `transaction.rollback()`，开发库数据原封不动。这让"测服务层真实 commit 行为"与"不留测试数据"两个矛盾诉求同时成立。

### 难点与解决
1. **settings 先读 .env 导致测试连错库**：conftest 必须在 `import app` **之前**设置 `os.environ["DB_NAME"]="enterprise_test"`——pydantic-settings 中环境变量优先级高于 .env 文件，靠优先级覆盖而非改代码，实现"测试切换数据库不侵入应用"。
2. **HTTP 层测试的会话一致性**：`client` fixture 用 `app.dependency_overrides` 把 `get_db` 覆盖为同一个用例回滚会话，接口内写入与断言在同一事务内可见，结束一起回滚。
3. **纯函数优先**：NL2SQL 校验被刻意设计为纯函数（不触库），15 个注入/多语句/裸 OFFSET/LIMIT 改写用例无需任何数据库即可运行——**可测性是设计出来的，不是测出来的**。

---

## 第九章 全项目技术全景与反思

### 架构分层（贯穿始终的约定）
```
routers（路由+权限声明） → services（业务逻辑） → models（SQLAlchemy）
 schemas（Pydantic 入参）  utils（响应/分页/Fernet/Excel）   alembic（迁移+播种）
前端: api(request拦截器/SSE) → stores(zustand) → router(viewMap动态路由) → views(25页)
```
全局铁律：软删除、列表必分页、管理接口必挂 `require_permissions`、关键操作必写 `sys_log` 审计、金额 DECIMAL、密钥只进 .env。

### 安全设计清单（按攻击面）
| 攻击面 | 防线 |
| --- | --- |
| 密码 | bcrypt；连续失败 5 次锁 15 分钟；防账号枚举 |
| 令牌 | 双 Token + 刷新轮换 + jti 黑名单；前端并发刷新单例 |
| 找回 | 验证码 bcrypt 存储、双维度限流、试错 5 次作废、SMTP 可选 |
| SQL 注入（NL2SQL） | prompt 约束 → 字面量剥离+黑名单正则+表白名单 → 会话 READ ONLY → 独立连接 5s 超时，四层纵深 |
| 越权 | 后端依赖注入校验 + 403 审计；前端 HasPermission 只管显隐（不信任前端） |
| 文件上传 | 后缀白名单 + 魔数 + 50MB + 哈希去重 |
| LLM 触达系统 | server_admin 沙箱 + 白名单 action + 权限双门控；图片 Data URL 正则白名单 |
| 密钥 | Fernet 加密入库、掩码回显、.env 管理 |

### 最有价值的几条经验（从提交历史中提炼）
1. **外键成环、并发刷新、字面量误杀、缓存失效**这类问题都是动手后才暴露的——先跑通最小闭环、再用测试与回归收口，比一次性设计完美更可靠。
2. **兜底与降级是 AI 功能的一等公民**：rerank 挂了降级融合序、CRAG 分级失败放行、模型不支持视觉剥图重试、SMTP 失败回退回显、.env 兜底模型配置——外部依赖不可用时"退化而非崩溃"贯穿全部 AI 链路。
3. **统计口径与权限边界要在设计期定义**：M5 看板对账修复（20=13+7）与员工 AI 入口补授（`8857ada`）都是回归阶段才发现的口径/边界问题。
4. **可测性驱动设计**：NL2SQL 校验纯函数化、savepoint 事务收敛、依赖注入覆盖 get_db——最终 36 用例可以在不污染任何环境的前提下全绿。
5. **工程决策记录在提交信息里**：39 条提交每条都写清"做了什么+为什么+取舍"（如 lint 豁免的理由、快照与实时的分工），让提交历史本身成为可追溯的设计文档。
